{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "branch=$(git -C /home/user/Claude-Code rev-parse --abbrev-ref HEAD 2>/dev/null); if [ \"$branch\" = \"staging\" ]; then git -C /home/user/Claude-Code push origin staging 2>&1 | tail -3; fi",
            "statusMessage": "Deploying to Render Stage...",
            "async": true
          }
        ]
      }
    ]
  }
}
