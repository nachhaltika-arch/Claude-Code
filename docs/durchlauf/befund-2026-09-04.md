# Systemdurchlauf — Befund vom 2026-09-04

> **Dieser Bericht ist ein Vorschlag, keine Lueckenliste.** Was hier steht,
> ist gemessen; was daraus eine L-Nummer wird, entscheidest du. Erst danach
> wandert es in `docs/soll-ist-analyse.md` und ins Lagebild.

## Was gelaufen ist

| Stufe | Bedarf | Befunde | Grundgesamtheit |
|---|---|---:|---|
| Felder ohne Leser | liest nur den Quelltext | 8 | — |
| Modell ohne Migration | liest nur den Quelltext | 0 | 6 der letzten 60 Commits haben eine Modelldatei geaendert; 0 davon ohne Migration im selben Commit |
| SQL nennt unbekannte Tabelle | liest nur den Quelltext | 1 | — |
| Doppelte Routen | liest nur den Quelltext | 0 | — |
| Waechter laesst ohne Geheimnis durch | liest nur den Quelltext | 0 | — |
| Geheimnis in der Adresse | liest nur den Quelltext | 0 | — |
| Stiller Ausfall im Schreibpfad | liest nur den Quelltext | 11 | — |
| Seiten ohne Weg | liest nur den Quelltext | 0 | — |
| Bedienelement ohne Wirkung | liest nur den Quelltext | 7 | 215 JSX-Dateien gelesen |
| Rollendrift | liest nur den Quelltext | 0 | gueltig: admin, kunde, mitarbeiter, superadmin · Matrix: admin, kunde, mitarbeiter, superadmin · Oberflaeche: admin, kunde, mitarbeiter, superadmin · Altnamen: auditor, nutzer |
| Verwaltungsroute ohne Rolle | liest nur den Quelltext | 0 | 91 Routen in App.jsx geprueft |
| Bild ohne Alternativtext, Feld ohne Beschriftung | liest nur den Quelltext | 7 | 1 Bilder ohne alt, 46 Felder ohne Beschriftung |
| Farben ausserhalb der Vorgabe | liest nur den Quelltext | 19 | — |
| Tokens ohne Dunkelfassung | liest nur den Quelltext | 1 | 40 Farbtokens im Hellsatz, 37 im Dunkelsatz; 12 ohne Dunkelfassung, davon 8 tatsaechlich benutzt |
| Farbe statt Token | liest nur den Quelltext | 5 | 1037 feste Farbwerte in 127 Dateien |
| Token-Kontrast in beiden Themes | braucht node_modules | 0 | Token-Kontrast in beiden Themes bestanden (14 Prüfungen) |
| Messungen am gerenderten Bild | liest nur den Quelltext | 1 | 2 Werkzeuge vorhanden, keines gelaufen |
| Namensdrift Umgebung | liest nur den Quelltext | 0 | — |
| Umgebung ohne Blueprint | liest nur den Quelltext | 1 | — |
| Import ohne Eintrag | liest nur den Quelltext | 1 | 272 Backend-Dateien gelesen, 67 Pakete eingetragen |
| Prueftor mit Luecke | liest nur den Quelltext | 0 | — |
| Termine fremder Dienste | liest nur den Quelltext | 1 | 1 Termine gefuehrt, 1 faellig oder abgelaufen |
| Dateien ueber der Grenze | liest nur den Quelltext | 3 | — |
| Laufzeit (Browser) | braucht einen laufenden Dienst | 2 | 40 Seiten gegen https://kompagnon-frontend-staging.onrender.com |
| **Summe** | | **68** | |

Davon **65 neu**, 3 bereits als L-Nummer gefuehrt, 0 frueher abgeraeumt.

### Nicht gemessen — und das ist nicht dasselbe wie in Ordnung

* **Routen ohne Aufrufer / ohne Anmeldung** — kein Interpreter mit den Backend-Abhaengigkeiten gefunden (`kompagnon/backend/venv`). Diese Stufe misst an der geladenen Anwendung und sieht Dinge, die aus dem Quelltext nicht ableitbar sind — sie gehoert nachgeholt, nicht uebergangen.

## Zu pruefen: Gegenstand steht in einem erledigten Eintrag

Der Abgleich ist ein **Textvergleich**, kein Urteil: Der Gegenstand
kommt in einer Zeile vor, die als erledigt gefuehrt wird. Das ist
entweder ein Rueckfall — dann der wichtigste Befund des Laufs — oder
der Eintrag erwaehnt die Sache nur nebenbei.

| Ebene | Befund | Beleg | steht unter |
|---|---|---|---|
| optik | #003840 liegt dicht neben der Vorgabe #002535 (5 Stellen) | `kompagnon/frontend/src/styles/tokens.css · kompagnon/frontend/src/utils/markenfarben.test.js` | L-32 |
| konsistenz | `BACKEND_URL` wird an 2 Stellen gelesen, steht in keinem Blueprint | `kompagnon/backend/routers/projects_anlegen.py · kompagnon/backend/routers/shop.py` | L-156 |

## Neue Befunde

### Ebene 1 — Datenbank

**kompagnon/backend/routers/projects_sichtbarkeit.py schreibt auf website_content — kein Modell fuehrt diese Tabelle** · Vorschlag P2

Entweder ist der Tabellenname falsch geschrieben — dann scheitert die Anweisung erst zur Laufzeit, an einer Stelle, die selten laeuft —, oder die Tabelle ist ausserhalb der Modelle entstanden und wird von keinem Werkzeug mitgefuehrt.

*Beleg:* `kompagnon/backend/routers/projects_sichtbarkeit.py — INSERT/UPDATE/DELETE auf website_content`

*Kennung:* `sql-unbekannte-tabelle/kompagnon/backend/routers/projects_sichtbarkeit.py` — in `docs/durchlauf/quittiert.json` eintragen, um den Befund dauerhaft abzuraeumen.

**`contact_address` (modelle_crawler.py) kommt ausserhalb des Modells nirgends vor** · Vorschlag P3

Entweder ist die Spalte tote Last, oder ein Lesepfad fehlt. Dieselbe Familie wie L-55: `AcademyModule.is_locked` war gespeichert, anklickbar, serialisiert — und nirgends gelesen.

*Beleg:* `kompagnon/backend/modelle_crawler.py — kein Treffer in Backend ausserhalb der Modelle und in 342 Frontend-Dateien`

*Kennung:* `feld-ohne-leser/modelle_crawler.contact_address` — in `docs/durchlauf/quittiert.json` eintragen, um den Befund dauerhaft abzuraeumen.

**`h2_list` (modelle_crawler.py) kommt ausserhalb des Modells nirgends vor** · Vorschlag P3

Entweder ist die Spalte tote Last, oder ein Lesepfad fehlt. Dieselbe Familie wie L-55: `AcademyModule.is_locked` war gespeichert, anklickbar, serialisiert — und nirgends gelesen.

*Beleg:* `kompagnon/backend/modelle_crawler.py — kein Treffer in Backend ausserhalb der Modelle und in 342 Frontend-Dateien`

*Kennung:* `feld-ohne-leser/modelle_crawler.h2_list` — in `docs/durchlauf/quittiert.json` eintragen, um den Befund dauerhaft abzuraeumen.

**`paragraphs` (modelle_crawler.py) kommt ausserhalb des Modells nirgends vor** · Vorschlag P3

Entweder ist die Spalte tote Last, oder ein Lesepfad fehlt. Dieselbe Familie wie L-55: `AcademyModule.is_locked` war gespeichert, anklickbar, serialisiert — und nirgends gelesen.

*Beleg:* `kompagnon/backend/modelle_crawler.py — kein Treffer in Backend ausserhalb der Modelle und in 342 Frontend-Dateien`

*Kennung:* `feld-ohne-leser/modelle_crawler.paragraphs` — in `docs/durchlauf/quittiert.json` eintragen, um den Befund dauerhaft abzuraeumen.

**`total_pages` (modelle_crawler.py) kommt ausserhalb des Modells nirgends vor** · Vorschlag P3

Entweder ist die Spalte tote Last, oder ein Lesepfad fehlt. Dieselbe Familie wie L-55: `AcademyModule.is_locked` war gespeichert, anklickbar, serialisiert — und nirgends gelesen.

*Beleg:* `kompagnon/backend/modelle_crawler.py — kein Treffer in Backend ausserhalb der Modelle und in 342 Frontend-Dateien`

*Kennung:* `feld-ohne-leser/modelle_crawler.total_pages` — in `docs/durchlauf/quittiert.json` eintragen, um den Befund dauerhaft abzuraeumen.

**`apple_id` (modelle_konten.py) kommt ausserhalb des Modells nirgends vor** · Vorschlag P3

Entweder ist die Spalte tote Last, oder ein Lesepfad fehlt. Dieselbe Familie wie L-55: `AcademyModule.is_locked` war gespeichert, anklickbar, serialisiert — und nirgends gelesen.

*Beleg:* `kompagnon/backend/modelle_konten.py — kein Treffer in Backend ausserhalb der Modelle und in 342 Frontend-Dateien`

*Kennung:* `feld-ohne-leser/modelle_konten.apple_id` — in `docs/durchlauf/quittiert.json` eintragen, um den Befund dauerhaft abzuraeumen.

**`google_id` (modelle_konten.py) kommt ausserhalb des Modells nirgends vor** · Vorschlag P3

Entweder ist die Spalte tote Last, oder ein Lesepfad fehlt. Dieselbe Familie wie L-55: `AcademyModule.is_locked` war gespeichert, anklickbar, serialisiert — und nirgends gelesen.

*Beleg:* `kompagnon/backend/modelle_konten.py — kein Treffer in Backend ausserhalb der Modelle und in 342 Frontend-Dateien`

*Kennung:* `feld-ohne-leser/modelle_konten.google_id` — in `docs/durchlauf/quittiert.json` eintragen, um den Befund dauerhaft abzuraeumen.

**`is_valid` (modelle_konten.py) kommt ausserhalb des Modells nirgends vor** · Vorschlag P3

Entweder ist die Spalte tote Last, oder ein Lesepfad fehlt. Dieselbe Familie wie L-55: `AcademyModule.is_locked` war gespeichert, anklickbar, serialisiert — und nirgends gelesen.

*Beleg:* `kompagnon/backend/modelle_konten.py — kein Treffer in Backend ausserhalb der Modelle und in 342 Frontend-Dateien`

*Kennung:* `feld-ohne-leser/modelle_konten.is_valid` — in `docs/durchlauf/quittiert.json` eintragen, um den Befund dauerhaft abzuraeumen.

**`oauth_provider` (modelle_konten.py) kommt ausserhalb des Modells nirgends vor** · Vorschlag P3

Entweder ist die Spalte tote Last, oder ein Lesepfad fehlt. Dieselbe Familie wie L-55: `AcademyModule.is_locked` war gespeichert, anklickbar, serialisiert — und nirgends gelesen.

*Beleg:* `kompagnon/backend/modelle_konten.py — kein Treffer in Backend ausserhalb der Modelle und in 342 Frontend-Dateien`

*Kennung:* `feld-ohne-leser/modelle_konten.oauth_provider` — in `docs/durchlauf/quittiert.json` eintragen, um den Befund dauerhaft abzuraeumen.

### Ebene 2 — Schnittstelle

**`stripe_webhook()` antwortet im Fehlerfall mit Erfolg — der Absender wiederholt nicht** · Vorschlag P0

**Der Abfangzweig endet mit einer Erfolgsantwort.** Bei einem Rueckruf von aussen ist das folgenschwerer als ein verschlucktes Protokoll: Stripe, Brevo und die Anzeigenportale werten 200 als „verarbeitet" und liefern **nie wieder**. Scheitert die Verarbeitung, ist der Vorgang endgueltig weg — die Zahlung ist gebucht, das Projekt entsteht nicht, und niemand erfaehrt es ausser einer Protokollzeile. Der Absender darf nur dann 200 bekommen, wenn der Vorgang **festgehalten** ist; alles andere gehoert mit 5xx beantwortet, damit der Rueckruf wiederholt wird. **Hier haengt Geld daran** — deshalb P0. Familie L-36, L-141.

*Beleg:* `kompagnon/backend/routers/payments.py:348`

*Kennung:* `stiller-ausfall/payments.stripe_webhook` — in `docs/durchlauf/quittiert.json` eintragen, um den Befund dauerhaft abzuraeumen.

**`forgot_password()` faengt jeden Fehler ab und meldet trotzdem Erfolg** · Vorschlag P1

Ein schreibender Endpunkt in einem Bereich, in dem Geld, Konten oder fremde Rueckrufe haengen — mit einem breiten Abfangzweig, der nur protokolliert, keine Vorbelegung auffaengt und keinen Kommentar traegt. Der Aufrufer bekommt Erfolg, die Handlung ist nicht passiert. Familie L-36, L-141.

*Beleg:* `kompagnon/backend/routers/auth_router.py:522`

*Kennung:* `stiller-ausfall/auth_router.forgot_password` — in `docs/durchlauf/quittiert.json` eintragen, um den Befund dauerhaft abzuraeumen.

**`audit_anfrage()` antwortet im Fehlerfall mit Erfolg** · Vorschlag P1

**Der Abfangzweig endet mit einer Erfolgsantwort an das eigene Frontend.** Der Nutzer sieht, dass es geklappt hat; passiert ist nichts. Kein Datenverlust wie beim Rueckruf von aussen — der Vorgang laesst sich wiederholen —, aber niemand weiss, dass er es muss. Die Antwort sollte sagen, was wirklich geschehen ist, wie es `auth_router.register` mit `versandt` vormacht. Familie L-36, L-141.

*Beleg:* `kompagnon/backend/routers/kampagne.py:201`

*Kennung:* `stiller-ausfall/kampagne.audit_anfrage` — in `docs/durchlauf/quittiert.json` eintragen, um den Befund dauerhaft abzuraeumen.

**`send_email_endpoint()` antwortet im Fehlerfall mit Erfolg** · Vorschlag P1

**Der Abfangzweig endet mit einer Erfolgsantwort an das eigene Frontend.** Der Nutzer sieht, dass es geklappt hat; passiert ist nichts. Kein Datenverlust wie beim Rueckruf von aussen — der Vorgang laesst sich wiederholen —, aber niemand weiss, dass er es muss. Die Antwort sollte sagen, was wirklich geschehen ist, wie es `auth_router.register` mit `versandt` vormacht. Familie L-36, L-141.

*Beleg:* `kompagnon/backend/routers/messages.py:169`

*Kennung:* `stiller-ausfall/messages.send_email_endpoint` — in `docs/durchlauf/quittiert.json` eintragen, um den Befund dauerhaft abzuraeumen.

**`request_approval()` antwortet im Fehlerfall mit Erfolg** · Vorschlag P1

**Der Abfangzweig endet mit einer Erfolgsantwort an das eigene Frontend.** Der Nutzer sieht, dass es geklappt hat; passiert ist nichts. Kein Datenverlust wie beim Rueckruf von aussen — der Vorgang laesst sich wiederholen —, aber niemand weiss, dass er es muss. Die Antwort sollte sagen, was wirklich geschehen ist, wie es `auth_router.register` mit `versandt` vormacht. Familie L-36, L-141.

*Beleg:* `kompagnon/backend/routers/projects.py:712`

*Kennung:* `stiller-ausfall/projects.request_approval` — in `docs/durchlauf/quittiert.json` eintragen, um den Befund dauerhaft abzuraeumen.

**`webhook_facebook()` antwortet im Fehlerfall mit Erfolg — der Absender wiederholt nicht** · Vorschlag P1

**Der Abfangzweig endet mit einer Erfolgsantwort.** Bei einem Rueckruf von aussen ist das folgenschwerer als ein verschlucktes Protokoll: Stripe, Brevo und die Anzeigenportale werten 200 als „verarbeitet" und liefern **nie wieder**. Scheitert die Verarbeitung, ist der Vorgang endgueltig weg — die Zahlung ist gebucht, das Projekt entsteht nicht, und niemand erfaehrt es ausser einer Protokollzeile. Der Absender darf nur dann 200 bekommen, wenn der Vorgang **festgehalten** ist; alles andere gehoert mit 5xx beantwortet, damit der Rueckruf wiederholt wird. **Es kann Absicht sein**: Manche Anzeigenportale schalten einen Rueckruf ab, der Fehler liefert. Dann ist die Antwort richtig und es fehlt nur der Satz daneben, der das sagt — schreib ihn hin, dann meldet der naechste Lauf die Stelle nicht mehr. Familie L-36, L-141.

*Beleg:* `kompagnon/backend/routers/webhooks.py:127`

*Kennung:* `stiller-ausfall/webhooks.webhook_facebook` — in `docs/durchlauf/quittiert.json` eintragen, um den Befund dauerhaft abzuraeumen.

**`webhook_linkedin()` antwortet im Fehlerfall mit Erfolg — der Absender wiederholt nicht** · Vorschlag P1

**Der Abfangzweig endet mit einer Erfolgsantwort.** Bei einem Rueckruf von aussen ist das folgenschwerer als ein verschlucktes Protokoll: Stripe, Brevo und die Anzeigenportale werten 200 als „verarbeitet" und liefern **nie wieder**. Scheitert die Verarbeitung, ist der Vorgang endgueltig weg — die Zahlung ist gebucht, das Projekt entsteht nicht, und niemand erfaehrt es ausser einer Protokollzeile. Der Absender darf nur dann 200 bekommen, wenn der Vorgang **festgehalten** ist; alles andere gehoert mit 5xx beantwortet, damit der Rueckruf wiederholt wird. **Es kann Absicht sein**: Manche Anzeigenportale schalten einen Rueckruf ab, der Fehler liefert. Dann ist die Antwort richtig und es fehlt nur der Satz daneben, der das sagt — schreib ihn hin, dann meldet der naechste Lauf die Stelle nicht mehr. Familie L-36, L-141.

*Beleg:* `kompagnon/backend/routers/webhooks.py:140`

*Kennung:* `stiller-ausfall/webhooks.webhook_linkedin` — in `docs/durchlauf/quittiert.json` eintragen, um den Befund dauerhaft abzuraeumen.

**`webhook_google()` antwortet im Fehlerfall mit Erfolg — der Absender wiederholt nicht** · Vorschlag P1

**Der Abfangzweig endet mit einer Erfolgsantwort.** Bei einem Rueckruf von aussen ist das folgenschwerer als ein verschlucktes Protokoll: Stripe, Brevo und die Anzeigenportale werten 200 als „verarbeitet" und liefern **nie wieder**. Scheitert die Verarbeitung, ist der Vorgang endgueltig weg — die Zahlung ist gebucht, das Projekt entsteht nicht, und niemand erfaehrt es ausser einer Protokollzeile. Der Absender darf nur dann 200 bekommen, wenn der Vorgang **festgehalten** ist; alles andere gehoert mit 5xx beantwortet, damit der Rueckruf wiederholt wird. **Es kann Absicht sein**: Manche Anzeigenportale schalten einen Rueckruf ab, der Fehler liefert. Dann ist die Antwort richtig und es fehlt nur der Satz daneben, der das sagt — schreib ihn hin, dann meldet der naechste Lauf die Stelle nicht mehr. Familie L-36, L-141.

*Beleg:* `kompagnon/backend/routers/webhooks.py:155`

*Kennung:* `stiller-ausfall/webhooks.webhook_google` — in `docs/durchlauf/quittiert.json` eintragen, um den Befund dauerhaft abzuraeumen.

**`webhook_postkarte()` antwortet im Fehlerfall mit Erfolg — der Absender wiederholt nicht** · Vorschlag P1

**Der Abfangzweig endet mit einer Erfolgsantwort.** Bei einem Rueckruf von aussen ist das folgenschwerer als ein verschlucktes Protokoll: Stripe, Brevo und die Anzeigenportale werten 200 als „verarbeitet" und liefern **nie wieder**. Scheitert die Verarbeitung, ist der Vorgang endgueltig weg — die Zahlung ist gebucht, das Projekt entsteht nicht, und niemand erfaehrt es ausser einer Protokollzeile. Der Absender darf nur dann 200 bekommen, wenn der Vorgang **festgehalten** ist; alles andere gehoert mit 5xx beantwortet, damit der Rueckruf wiederholt wird. **Es kann Absicht sein**: Manche Anzeigenportale schalten einen Rueckruf ab, der Fehler liefert. Dann ist die Antwort richtig und es fehlt nur der Satz daneben, der das sagt — schreib ihn hin, dann meldet der naechste Lauf die Stelle nicht mehr. Familie L-36, L-141.

*Beleg:* `kompagnon/backend/routers/webhooks.py:167`

*Kennung:* `stiller-ausfall/webhooks.webhook_postkarte` — in `docs/durchlauf/quittiert.json` eintragen, um den Befund dauerhaft abzuraeumen.

**`webhook_telefon()` antwortet im Fehlerfall mit Erfolg — der Absender wiederholt nicht** · Vorschlag P1

**Der Abfangzweig endet mit einer Erfolgsantwort.** Bei einem Rueckruf von aussen ist das folgenschwerer als ein verschlucktes Protokoll: Stripe, Brevo und die Anzeigenportale werten 200 als „verarbeitet" und liefern **nie wieder**. Scheitert die Verarbeitung, ist der Vorgang endgueltig weg — die Zahlung ist gebucht, das Projekt entsteht nicht, und niemand erfaehrt es ausser einer Protokollzeile. Der Absender darf nur dann 200 bekommen, wenn der Vorgang **festgehalten** ist; alles andere gehoert mit 5xx beantwortet, damit der Rueckruf wiederholt wird. **Es kann Absicht sein**: Manche Anzeigenportale schalten einen Rueckruf ab, der Fehler liefert. Dann ist die Antwort richtig und es fehlt nur der Satz daneben, der das sagt — schreib ihn hin, dann meldet der naechste Lauf die Stelle nicht mehr. Familie L-36, L-141.

*Beleg:* `kompagnon/backend/routers/webhooks.py:181`

*Kennung:* `stiller-ausfall/webhooks.webhook_telefon` — in `docs/durchlauf/quittiert.json` eintragen, um den Befund dauerhaft abzuraeumen.

**17 schreibende Endpunkte schweigen unerklaert ueber Fehler** · Vorschlag P2

Breiter Abfangzweig, nur ein Protokolleintrag, keine Vorbelegung, die ihn auffaengt, und kein Kommentar, der ihn begruendet. **Das ist keine Liste von Maengeln, sondern eine Liste von Fragen:** Bei jeder Stelle ist zu klaeren, ob das Schweigen Absicht ist. Ist es Absicht, gehoert der Grund als Kommentar daneben — dann verschwindet die Zeile beim naechsten Lauf von selbst. Ist es keine, gehoert der Fehler an den Aufrufer zurueck. Die Stufe zaehlt bewusst die **unerklaerten**: Wo eine Begruendung steht, meldet sie nichts.

*Beleg:* `audit.py:526 (start_audit) · branddesign_erhebung.py:153 (scrape_brand) · briefings_ki.py:80 (suggest_field) · briefings_ki.py:281 (ki_prefill_seo) · briefings_ki.py:367 (ki_prefill_ziele) · files.py:326 (delete_file)`

*Kennung:* `stiller-ausfall/sammel` — in `docs/durchlauf/quittiert.json` eintragen, um den Befund dauerhaft abzuraeumen.

### Ebene 3 — Frontend

**BrandDesignEditor.jsx: 2 Schaltflaeche(n) ohne Handler und ohne Formularrolle** · Vorschlag P2

Ein `<button>` ohne `onClick`, ohne `type="submit"` und ohne `form=` kann nichts ausloesen. Das ist keine Vermutung, sondern eine Eigenschaft des Markups — der Knopf sieht aus wie ein Angebot und ist keines. L-79 war genau das. **Zwei Ausnahmen sind haeufig und berechtigt:** ein Knopf in einer Vorschau, die zeigt, wie etwas aussehen wuerde, und einer, der nur als Anzeige dient. Beide gehoeren mit ihrem Grund ins Quittungsjournal — dann meldet der naechste Lauf die Datei nicht mehr, und die Zahl bedeutet wieder etwas.

*Beleg:* `kompagnon/frontend/src/components/BrandDesignEditor.jsx — Zeile 199, 213`

*Kennung:* `knopf-ohne-wirkung/kompagnon/frontend/src/components/BrandDesignEditor.jsx` — in `docs/durchlauf/quittiert.json` eintragen, um den Befund dauerhaft abzuraeumen.

**BrandDesignWerkstatt.jsx: 2 Schaltflaeche(n) ohne Handler und ohne Formularrolle** · Vorschlag P2

Ein `<button>` ohne `onClick`, ohne `type="submit"` und ohne `form=` kann nichts ausloesen. Das ist keine Vermutung, sondern eine Eigenschaft des Markups — der Knopf sieht aus wie ein Angebot und ist keines. L-79 war genau das. **Zwei Ausnahmen sind haeufig und berechtigt:** ein Knopf in einer Vorschau, die zeigt, wie etwas aussehen wuerde, und einer, der nur als Anzeige dient. Beide gehoeren mit ihrem Grund ins Quittungsjournal — dann meldet der naechste Lauf die Datei nicht mehr, und die Zahl bedeutet wieder etwas.

*Beleg:* `kompagnon/frontend/src/components/BrandDesignWerkstatt.jsx — Zeile 295, 306`

*Kennung:* `knopf-ohne-wirkung/kompagnon/frontend/src/components/BrandDesignWerkstatt.jsx` — in `docs/durchlauf/quittiert.json` eintragen, um den Befund dauerhaft abzuraeumen.

**BrandGuideline.jsx: 4 Schaltflaeche(n) ohne Handler und ohne Formularrolle** · Vorschlag P2

Ein `<button>` ohne `onClick`, ohne `type="submit"` und ohne `form=` kann nichts ausloesen. Das ist keine Vermutung, sondern eine Eigenschaft des Markups — der Knopf sieht aus wie ein Angebot und ist keines. L-79 war genau das. **Zwei Ausnahmen sind haeufig und berechtigt:** ein Knopf in einer Vorschau, die zeigt, wie etwas aussehen wuerde, und einer, der nur als Anzeige dient. Beide gehoeren mit ihrem Grund ins Quittungsjournal — dann meldet der naechste Lauf die Datei nicht mehr, und die Zahl bedeutet wieder etwas.

*Beleg:* `kompagnon/frontend/src/components/BrandGuideline.jsx — Zeile 377, 466, 486, 493`

*Kennung:* `knopf-ohne-wirkung/kompagnon/frontend/src/components/BrandGuideline.jsx` — in `docs/durchlauf/quittiert.json` eintragen, um den Befund dauerhaft abzuraeumen.

**styleguideTeile.jsx: 3 Schaltflaeche(n) ohne Handler und ohne Formularrolle** · Vorschlag P2

Ein `<button>` ohne `onClick`, ohne `type="submit"` und ohne `form=` kann nichts ausloesen. Das ist keine Vermutung, sondern eine Eigenschaft des Markups — der Knopf sieht aus wie ein Angebot und ist keines. L-79 war genau das. **Zwei Ausnahmen sind haeufig und berechtigt:** ein Knopf in einer Vorschau, die zeigt, wie etwas aussehen wuerde, und einer, der nur als Anzeige dient. Beide gehoeren mit ihrem Grund ins Quittungsjournal — dann meldet der naechste Lauf die Datei nicht mehr, und die Zahl bedeutet wieder etwas.

*Beleg:* `kompagnon/frontend/src/components/styleguide/styleguideTeile.jsx — Zeile 618, 624, 756`

*Kennung:* `knopf-ohne-wirkung/kompagnon/frontend/src/components/styleguide/styleguideTeile.jsx` — in `docs/durchlauf/quittiert.json` eintragen, um den Befund dauerhaft abzuraeumen.

**Register.jsx: 1 Schaltflaeche(n) ohne Handler und ohne Formularrolle** · Vorschlag P2

Ein `<button>` ohne `onClick`, ohne `type="submit"` und ohne `form=` kann nichts ausloesen. Das ist keine Vermutung, sondern eine Eigenschaft des Markups — der Knopf sieht aus wie ein Angebot und ist keines. L-79 war genau das. **Zwei Ausnahmen sind haeufig und berechtigt:** ein Knopf in einer Vorschau, die zeigt, wie etwas aussehen wuerde, und einer, der nur als Anzeige dient. Beide gehoeren mit ihrem Grund ins Quittungsjournal — dann meldet der naechste Lauf die Datei nicht mehr, und die Zahl bedeutet wieder etwas.

*Beleg:* `kompagnon/frontend/src/pages/Register.jsx — Zeile 117`

*Kennung:* `knopf-ohne-wirkung/kompagnon/frontend/src/pages/Register.jsx` — in `docs/durchlauf/quittiert.json` eintragen, um den Befund dauerhaft abzuraeumen.

**Settings.jsx: 1 Schaltflaeche(n) ohne Handler und ohne Formularrolle** · Vorschlag P2

Ein `<button>` ohne `onClick`, ohne `type="submit"` und ohne `form=` kann nichts ausloesen. Das ist keine Vermutung, sondern eine Eigenschaft des Markups — der Knopf sieht aus wie ein Angebot und ist keines. L-79 war genau das. **Zwei Ausnahmen sind haeufig und berechtigt:** ein Knopf in einer Vorschau, die zeigt, wie etwas aussehen wuerde, und einer, der nur als Anzeige dient. Beide gehoeren mit ihrem Grund ins Quittungsjournal — dann meldet der naechste Lauf die Datei nicht mehr, und die Zahl bedeutet wieder etwas.

*Beleg:* `kompagnon/frontend/src/pages/Settings.jsx — Zeile 352`

*Kennung:* `knopf-ohne-wirkung/kompagnon/frontend/src/pages/Settings.jsx` — in `docs/durchlauf/quittiert.json` eintragen, um den Befund dauerhaft abzuraeumen.

**TemplateLibrary.jsx: 1 Schaltflaeche(n) ohne Handler und ohne Formularrolle** · Vorschlag P2

Ein `<button>` ohne `onClick`, ohne `type="submit"` und ohne `form=` kann nichts ausloesen. Das ist keine Vermutung, sondern eine Eigenschaft des Markups — der Knopf sieht aus wie ein Angebot und ist keines. L-79 war genau das. **Zwei Ausnahmen sind haeufig und berechtigt:** ein Knopf in einer Vorschau, die zeigt, wie etwas aussehen wuerde, und einer, der nur als Anzeige dient. Beide gehoeren mit ihrem Grund ins Quittungsjournal — dann meldet der naechste Lauf die Datei nicht mehr, und die Zahl bedeutet wieder etwas.

*Beleg:* `kompagnon/frontend/src/pages/TemplateLibrary.jsx — Zeile 119`

*Kennung:* `knopf-ohne-wirkung/kompagnon/frontend/src/pages/TemplateLibrary.jsx` — in `docs/durchlauf/quittiert.json` eintragen, um den Befund dauerhaft abzuraeumen.

**AcademyAdminLesson.jsx: 1 Bild(er) ohne Alternativtext** · Vorschlag P2

Ein Bild ohne `alt` ist fuer eine Vorlesehilfe nicht vorhanden. Ist das Bild reine Zierde, gehoert `alt=""` hin — das ist die Aussage „hier ist nichts zu lesen" und etwas anderes als ein fehlendes Attribut. Massstab WCAG AA.

*Beleg:* `kompagnon/frontend/src/pages/AcademyAdminLesson.jsx — Zeile 301`

*Kennung:* `barrierefreiheit/bild-ohne-alt/kompagnon/frontend/src/pages/AcademyAdminLesson.jsx` — in `docs/durchlauf/quittiert.json` eintragen, um den Befund dauerhaft abzuraeumen.

**AcademyAdminLesson.jsx: 5 Eingabefeld(er) ohne Beschriftung** · Vorschlag P2

Ein Feld ohne `id`, `aria-label` oder Platzhalter hat fuer eine Vorlesehilfe keinen Namen. Der Nutzer hoert „Eingabefeld" und nichts weiter. Massstab WCAG AA.

*Beleg:* `kompagnon/frontend/src/pages/AcademyAdminLesson.jsx — Zeile 229, 572, 595, 617, 642`

*Kennung:* `barrierefreiheit/feld-ohne-beschriftung/kompagnon/frontend/src/pages/AcademyAdminLesson.jsx` — in `docs/durchlauf/quittiert.json` eintragen, um den Befund dauerhaft abzuraeumen.

**MitwirkungAktion.jsx: 3 Eingabefeld(er) ohne Beschriftung** · Vorschlag P2

Ein Feld ohne `id`, `aria-label` oder Platzhalter hat fuer eine Vorlesehilfe keinen Namen. Der Nutzer hoert „Eingabefeld" und nichts weiter. Massstab WCAG AA.

*Beleg:* `kompagnon/frontend/src/components/kunde/MitwirkungAktion.jsx — Zeile 94, 105, 180`

*Kennung:* `barrierefreiheit/feld-ohne-beschriftung/kompagnon/frontend/src/components/kunde/MitwirkungAktion.jsx` — in `docs/durchlauf/quittiert.json` eintragen, um den Befund dauerhaft abzuraeumen.

**AboZeiten.jsx: 3 Eingabefeld(er) ohne Beschriftung** · Vorschlag P2

Ein Feld ohne `id`, `aria-label` oder Platzhalter hat fuer eine Vorlesehilfe keinen Namen. Der Nutzer hoert „Eingabefeld" und nichts weiter. Massstab WCAG AA.

*Beleg:* `kompagnon/frontend/src/components/betrieb/AboZeiten.jsx — Zeile 152, 164, 175`

*Kennung:* `barrierefreiheit/feld-ohne-beschriftung/kompagnon/frontend/src/components/betrieb/AboZeiten.jsx` — in `docs/durchlauf/quittiert.json` eintragen, um den Befund dauerhaft abzuraeumen.

**AcademyAdminCourse.jsx: 3 Eingabefeld(er) ohne Beschriftung** · Vorschlag P2

Ein Feld ohne `id`, `aria-label` oder Platzhalter hat fuer eine Vorlesehilfe keinen Namen. Der Nutzer hoert „Eingabefeld" und nichts weiter. Massstab WCAG AA.

*Beleg:* `kompagnon/frontend/src/pages/AcademyAdminCourse.jsx — Zeile 280, 330, 343`

*Kennung:* `barrierefreiheit/feld-ohne-beschriftung/kompagnon/frontend/src/pages/AcademyAdminCourse.jsx` — in `docs/durchlauf/quittiert.json` eintragen, um den Befund dauerhaft abzuraeumen.

**ProductEditor.jsx: 3 Eingabefeld(er) ohne Beschriftung** · Vorschlag P2

Ein Feld ohne `id`, `aria-label` oder Platzhalter hat fuer eine Vorlesehilfe keinen Namen. Der Nutzer hoert „Eingabefeld" und nichts weiter. Massstab WCAG AA.

*Beleg:* `kompagnon/frontend/src/pages/ProductEditor.jsx — Zeile 245, 339, 348`

*Kennung:* `barrierefreiheit/feld-ohne-beschriftung/kompagnon/frontend/src/pages/ProductEditor.jsx` — in `docs/durchlauf/quittiert.json` eintragen, um den Befund dauerhaft abzuraeumen.

**Settings.jsx: 3 Eingabefeld(er) ohne Beschriftung** · Vorschlag P2

Ein Feld ohne `id`, `aria-label` oder Platzhalter hat fuer eine Vorlesehilfe keinen Namen. Der Nutzer hoert „Eingabefeld" und nichts weiter. Massstab WCAG AA.

*Beleg:* `kompagnon/frontend/src/pages/Settings.jsx — Zeile 176, 179, 305`

*Kennung:* `barrierefreiheit/feld-ohne-beschriftung/kompagnon/frontend/src/pages/Settings.jsx` — in `docs/durchlauf/quittiert.json` eintragen, um den Befund dauerhaft abzuraeumen.

### Optik

**1037 feste Farbwerte in 127 Dateien gehen beim Umschalten nicht mit** · Vorschlag P1

Der Dunkelmodus tauscht **Tokens**, keine Zeichenketten. Jede dieser Farben bleibt beim Umschalten stehen. **Das ist eine Entscheidung, nicht tausend:** Entweder die Oberflaeche wird durchgaengig auf Tokens gezogen — dann ist der Dunkelmodus belastbar —, oder es wird festgehalten, welche Bereiche bewusst nur hell gedacht sind. Der heutige Zustand ist der dritte Fall: Es ist nicht entschieden, und man sieht es erst beim Umschalten. Die schlimmsten Dateien stehen einzeln darunter.

*Beleg:* `haeufigste: SitemapPlaner.jsx (56×), LeadProfile.jsx (37×), SectionDetailPanel.jsx (35×), TemplateLibrary.jsx (30×), ContentManager.jsx (24×), AiGeneratorModal.jsx (24×), KampagneLandingPage.jsx (21×), PackagePremium.jsx (21×)`

*Kennung:* `theme/farbe-statt-token` — in `docs/durchlauf/quittiert.json` eintragen, um den Befund dauerhaft abzuraeumen.

**Im Dunkelmodus sind die Dashboard-Überschriften praktisch unsichtbar — 1,29:1** · Vorschlag P1

**`--kc-dark` ist eine Flächenfarbe, keine Schriftfarbe.** `tokens.css` benutzt sie als `--bg-sidebar` und als `--brand-primary` im Hellsatz; im Dunkelsatz wird sie auf #003840 gesetzt — noch dunkler, weil sie dort eine dunkle Fläche sein soll. `Dashboard.jsx` setzt sie als `color:`. Im Hellmodus geht das gut aus, weil dunkles Petrol auf hellem Grund liegt; im Dunkelmodus steht dunkles Petrol auf fast schwarzem Grund. Betroffen sind die Überschrift „Dashboard", die Kennzahl darunter und die Kartentitel „Aktuelle Betriebe" und „Letzte Audits" — also genau das, woran man sich auf der Seite orientiert. **Warum die Token-Kontrasttests das nicht fangen:** Sie rechnen eine ausdrückliche Paarliste, und `--kc-dark` als Textfarbe steht zu Recht nicht darin — es soll keine sein. Der Fehler liegt in der Verwendung, und den sieht nur die gerenderte Messung. Das ist der Satz aus `bedienbarkeit_messen.py`, hier belegt: Eine Farbe kann als Token bestehen und auf der Seite trotzdem auf dem falschen Grund landen.

*Beleg:* `kompagnon/frontend/src/pages/Dashboard.jsx:350 setzt color: var(--kc-dark); im Dunkelsatz ist --kc-dark = #003840 auf --surface #152028 → 1.29:1 bei 28 px (Schwelle 3,0) und 1.36:1 bei 13 px (Schwelle 4,5). Gemessen am 04.09.2026 im Browser gegen Staging, Theme über kas-theme umgeschaltet und Seite neu aufgebaut.`

*Kennung:* `kontrast/dark/app/dashboard/kc-dark-als-schrift` — in `docs/durchlauf/quittiert.json` eintragen, um den Befund dauerhaft abzuraeumen.

**Markenfarbe #004f59 wird 19-mal benutzt, steht aber in keiner Farbvorgabe** · Vorschlag P2

Die Farbe gehoert zur Marke, aber `tailwind.config.js` kennt sie nicht. Damit ist sie im Werkzeug Handarbeit statt Vorgabe: Sie laesst sich nicht zentral aendern, nicht pruefen und nicht wiederverwenden — und der naechste Bildschirm bekommt einen Ton daneben.

*Beleg:* `kompagnon/frontend/src/components/BrandDesignWerkstatt.jsx · kompagnon/frontend/src/components/DesignStudio.jsx · kompagnon/frontend/src/components/KASSidebar.jsx · kompagnon/frontend/src/components/schritte/marketing.jsx`

*Kennung:* `markenfarbe-ohne-vorgabe/#004f59` — in `docs/durchlauf/quittiert.json` eintragen, um den Befund dauerhaft abzuraeumen.

**Markenfarbe #fae600 wird 15-mal benutzt, steht aber in keiner Farbvorgabe** · Vorschlag P2

Die Farbe gehoert zur Marke, aber `tailwind.config.js` kennt sie nicht. Damit ist sie im Werkzeug Handarbeit statt Vorgabe: Sie laesst sich nicht zentral aendern, nicht pruefen und nicht wiederverwenden — und der naechste Bildschirm bekommt einen Ton daneben.

*Beleg:* `kompagnon/frontend/src/components/AnrechnungsHinweis.jsx · kompagnon/frontend/src/components/BrandDesignWerkstatt.jsx · kompagnon/frontend/src/components/KASSidebar.jsx · kompagnon/frontend/src/components/Mitwirkung.jsx`

*Kennung:* `markenfarbe-ohne-vorgabe/#fae600` — in `docs/durchlauf/quittiert.json` eintragen, um den Befund dauerhaft abzuraeumen.

**#065f46 liegt dicht neben der Vorgabe #004f59 (12 Stellen)** · Vorschlag P2

Abstand 26 von 442 im RGB-Raum. Eine Farbe so dicht an der Marke ist im Bildschirmfoto nicht zu unterscheiden und faellt erst auf, wenn zwei Flaechen nebeneinander liegen. Entweder ist sie ein Tippfehler, oder die Vorgabe kennt einen Ton, den sie nicht fuehrt.

*Beleg:* `kompagnon/frontend/src/components/ContentManager.jsx · kompagnon/frontend/src/components/DesignStudio.jsx · kompagnon/frontend/src/pages/PageManager.jsx · kompagnon/frontend/src/pages/PublicPageEditor.jsx`

*Kennung:* `farbe-beinahe/#065f46` — in `docs/durchlauf/quittiert.json` eintragen, um den Befund dauerhaft abzuraeumen.

**#1a2c32 liegt dicht neben der Vorgabe #002535 (11 Stellen)** · Vorschlag P2

Abstand 27 von 442 im RGB-Raum. Eine Farbe so dicht an der Marke ist im Bildschirmfoto nicht zu unterscheiden und faellt erst auf, wenn zwei Flaechen nebeneinander liegen. Entweder ist sie ein Tippfehler, oder die Vorgabe kennt einen Ton, den sie nicht fuehrt.

*Beleg:* `kompagnon/frontend/src/components/SitemapPlaner.jsx · kompagnon/frontend/src/components/views/sitemapDialoge.jsx · kompagnon/frontend/src/pages/PageTemplateEditor.jsx · kompagnon/frontend/src/pages/PublicPageEditor.jsx`

*Kennung:* `farbe-beinahe/#1a2c32` — in `docs/durchlauf/quittiert.json` eintragen, um den Befund dauerhaft abzuraeumen.

**#0f172a liegt dicht neben der Vorgabe #002535 (10 Stellen)** · Vorschlag P2

Abstand 23 von 442 im RGB-Raum. Eine Farbe so dicht an der Marke ist im Bildschirmfoto nicht zu unterscheiden und faellt erst auf, wenn zwei Flaechen nebeneinander liegen. Entweder ist sie ein Tippfehler, oder die Vorgabe kennt einen Ton, den sie nicht fuehrt.

*Beleg:* `kompagnon/frontend/src/components/DesignStudio.jsx · kompagnon/frontend/src/components/bibliothek/AiGeneratorModal.jsx · kompagnon/frontend/src/components/styleguide/styleguideTeile.jsx · kompagnon/frontend/src/components/views/StyleGuideView.jsx`

*Kennung:* `farbe-beinahe/#0f172a` — in `docs/durchlauf/quittiert.json` eintragen, um den Befund dauerhaft abzuraeumen.

**#042f2e liegt dicht neben der Vorgabe #002535 (4 Stellen)** · Vorschlag P2

Abstand 13 von 442 im RGB-Raum. Eine Farbe so dicht an der Marke ist im Bildschirmfoto nicht zu unterscheiden und faellt erst auf, wenn zwei Flaechen nebeneinander liegen. Entweder ist sie ein Tippfehler, oder die Vorgabe kennt einen Ton, den sie nicht fuehrt.

*Beleg:* `kompagnon/frontend/src/components/views/StyleGuideView.jsx · kompagnon/frontend/src/utils/brandOverride.test.js`

*Kennung:* `farbe-beinahe/#042f2e` — in `docs/durchlauf/quittiert.json` eintragen, um den Befund dauerhaft abzuraeumen.

**#006680 liegt dicht neben der Vorgabe #007090 (4 Stellen)** · Vorschlag P2

Abstand 19 von 442 im RGB-Raum. Eine Farbe so dicht an der Marke ist im Bildschirmfoto nicht zu unterscheiden und faellt erst auf, wenn zwei Flaechen nebeneinander liegen. Entweder ist sie ein Tippfehler, oder die Vorgabe kennt einen Ton, den sie nicht fuehrt.

*Beleg:* `kompagnon/frontend/src/components/AnalyseCentrale.jsx · kompagnon/frontend/src/components/DesignStudio.jsx · kompagnon/frontend/src/pages/ScraperControl.jsx`

*Kennung:* `farbe-beinahe/#006680` — in `docs/durchlauf/quittiert.json` eintragen, um den Befund dauerhaft abzuraeumen.

**#006880 liegt dicht neben der Vorgabe #007090 (4 Stellen)** · Vorschlag P2

Abstand 18 von 442 im RGB-Raum. Eine Farbe so dicht an der Marke ist im Bildschirmfoto nicht zu unterscheiden und faellt erst auf, wenn zwei Flaechen nebeneinander liegen. Entweder ist sie ein Tippfehler, oder die Vorgabe kennt einen Ton, den sie nicht fuehrt.

*Beleg:* `kompagnon/frontend/src/components/OfferTab.jsx · kompagnon/frontend/src/pages/PackageStarter.jsx`

*Kennung:* `farbe-beinahe/#006880` — in `docs/durchlauf/quittiert.json` eintragen, um den Befund dauerhaft abzuraeumen.

**#0f1c20 liegt dicht neben der Vorgabe #002535 (4 Stellen)** · Vorschlag P2

Abstand 27 von 442 im RGB-Raum. Eine Farbe so dicht an der Marke ist im Bildschirmfoto nicht zu unterscheiden und faellt erst auf, wenn zwei Flaechen nebeneinander liegen. Entweder ist sie ein Tippfehler, oder die Vorgabe kennt einen Ton, den sie nicht fuehrt.

*Beleg:* `kompagnon/frontend/src/styles/tokens.css`

*Kennung:* `farbe-beinahe/#0f1c20` — in `docs/durchlauf/quittiert.json` eintragen, um den Befund dauerhaft abzuraeumen.

**#0f1e3a liegt dicht neben der Vorgabe #002535 (3 Stellen)** · Vorschlag P2

Abstand 17 von 442 im RGB-Raum. Eine Farbe so dicht an der Marke ist im Bildschirmfoto nicht zu unterscheiden und faellt erst auf, wenn zwei Flaechen nebeneinander liegen. Entweder ist sie ein Tippfehler, oder die Vorgabe kennt einen Ton, den sie nicht fuehrt.

*Beleg:* `kompagnon/frontend/src/components/OfferTab.jsx · kompagnon/frontend/src/pages/CheckoutSuccess.jsx`

*Kennung:* `farbe-beinahe/#0f1e3a` — in `docs/durchlauf/quittiert.json` eintragen, um den Befund dauerhaft abzuraeumen.

**#1a1a2e liegt dicht neben der Vorgabe #002535 (3 Stellen)** · Vorschlag P2

Abstand 29 von 442 im RGB-Raum. Eine Farbe so dicht an der Marke ist im Bildschirmfoto nicht zu unterscheiden und faellt erst auf, wenn zwei Flaechen nebeneinander liegen. Entweder ist sie ein Tippfehler, oder die Vorgabe kennt einen Ton, den sie nicht fuehrt.

*Beleg:* `kompagnon/frontend/src/components/BrandGuideline.jsx · kompagnon/frontend/src/components/schritte/marketing.jsx`

*Kennung:* `farbe-beinahe/#1a1a2e` — in `docs/durchlauf/quittiert.json` eintragen, um den Befund dauerhaft abzuraeumen.

**#0369a1 liegt dicht neben der Vorgabe #007090 (3 Stellen)** · Vorschlag P2

Abstand 19 von 442 im RGB-Raum. Eine Farbe so dicht an der Marke ist im Bildschirmfoto nicht zu unterscheiden und faellt erst auf, wenn zwei Flaechen nebeneinander liegen. Entweder ist sie ein Tippfehler, oder die Vorgabe kennt einen Ton, den sie nicht fuehrt.

*Beleg:* `kompagnon/frontend/src/components/MoodboardPanel.jsx · kompagnon/frontend/src/components/SecurityChecklist.jsx · kompagnon/frontend/src/pages/QRGenerator.jsx`

*Kennung:* `farbe-beinahe/#0369a1` — in `docs/durchlauf/quittiert.json` eintragen, um den Befund dauerhaft abzuraeumen.

**#0e7490 liegt dicht neben der Vorgabe #007090 (3 Stellen)** · Vorschlag P2

Abstand 15 von 442 im RGB-Raum. Eine Farbe so dicht an der Marke ist im Bildschirmfoto nicht zu unterscheiden und faellt erst auf, wenn zwei Flaechen nebeneinander liegen. Entweder ist sie ein Tippfehler, oder die Vorgabe kennt einen Ton, den sie nicht fuehrt.

*Beleg:* `kompagnon/frontend/src/components/views/StyleGuideView.jsx · kompagnon/frontend/src/components/views/sitemapKarten.jsx`

*Kennung:* `farbe-beinahe/#0e7490` — in `docs/durchlauf/quittiert.json` eintragen, um den Befund dauerhaft abzuraeumen.

**#111827 liegt dicht neben der Vorgabe #002535 (3 Stellen)** · Vorschlag P2

Abstand 26 von 442 im RGB-Raum. Eine Farbe so dicht an der Marke ist im Bildschirmfoto nicht zu unterscheiden und faellt erst auf, wenn zwei Flaechen nebeneinander liegen. Entweder ist sie ein Tippfehler, oder die Vorgabe kennt einen Ton, den sie nicht fuehrt.

*Beleg:* `kompagnon/frontend/src/grapesjs/handwerk-blocks.js`

*Kennung:* `farbe-beinahe/#111827` — in `docs/durchlauf/quittiert.json` eintragen, um den Befund dauerhaft abzuraeumen.

**#00778f liegt dicht neben der Vorgabe #007090 (3 Stellen)** · Vorschlag P2

Abstand 7 von 442 im RGB-Raum. Eine Farbe so dicht an der Marke ist im Bildschirmfoto nicht zu unterscheiden und faellt erst auf, wenn zwei Flaechen nebeneinander liegen. Entweder ist sie ein Tippfehler, oder die Vorgabe kennt einen Ton, den sie nicht fuehrt.

*Beleg:* `kompagnon/frontend/src/styles/tokens.css`

*Kennung:* `farbe-beinahe/#00778f` — in `docs/durchlauf/quittiert.json` eintragen, um den Befund dauerhaft abzuraeumen.

**8 benutzte Farbtokens haben keine Dunkelfassung — sie bleiben beim Umschalten hell** · Vorschlag P2

Der Hellsatz in `:root` gilt immer; der Dunkelsatz ueberschreibt nur, was anders sein soll. Was er auslaesst, bleibt hell — eine helle Flaeche oder helle Schrift mitten in einer dunklen Oberflaeche, und niemand sieht es, solange niemand umschaltet. **Nicht jedes davon ist ein Fehler:** Ein Gelb, das in beiden Faellen dasselbe sein soll, gehoert ausgelassen. Die Frage je Token lautet: absichtlich gleich, oder vergessen? Was absichtlich gleich ist, gehoert mit einem Kommentar daneben — dann ist es beim naechsten Mal keine Frage mehr.

*Beleg:* `kompagnon/frontend/src/styles/tokens.css — `--text-inverse` (34×), `--kc-mid-a-12` (14×), `--kc-black` (6×), `--kc-mid-a-30` (6×), `--kc-mid-a-25` (5×), `--kc-mid-a-20` (3×), `--kc-mid-a-08` (2×), `--kc-mid-a-50` (2×)`

*Kennung:* `theme/tokens-ohne-dunkelfassung` — in `docs/durchlauf/quittiert.json` eintragen, um den Befund dauerhaft abzuraeumen.

**SitemapPlaner.jsx: 56 feste Farbwerte, die beim Umschalten nicht mitgehen** · Vorschlag P2

Der Dunkelmodus tauscht **Tokens**, keine Zeichenketten. Eine Farbe, die als `'#…'` im Bauteil steht, bleibt beim Umschalten stehen: Die Flaeche wird dunkel, die Schrift bleibt fast schwarz. Das ist nicht nur haesslich, es ist unlesbar — und im Hellmodus faellt es nie auf. **Ausnahmen sind haeufig und berechtigt**: eine Markenvorschau, ein Diagramm mit eigener Farbskala, ein Bild in Bildpunkten. Die gehoeren quittiert. Der Rest gehoert an `var(--token)`. Wie L-32, wo dieselbe Aufraeumarbeit 39 Vorkommen in 21 Dateien betraf.

*Beleg:* `kompagnon/frontend/src/components/SitemapPlaner.jsx — Zeile 11, 14, 15, 16, 59, 59, 62, 102`

*Kennung:* `theme/farbe-statt-token/kompagnon/frontend/src/components/SitemapPlaner.jsx` — in `docs/durchlauf/quittiert.json` eintragen, um den Befund dauerhaft abzuraeumen.

**LeadProfile.jsx: 37 feste Farbwerte, die beim Umschalten nicht mitgehen** · Vorschlag P2

Der Dunkelmodus tauscht **Tokens**, keine Zeichenketten. Eine Farbe, die als `'#…'` im Bauteil steht, bleibt beim Umschalten stehen: Die Flaeche wird dunkel, die Schrift bleibt fast schwarz. Das ist nicht nur haesslich, es ist unlesbar — und im Hellmodus faellt es nie auf. **Ausnahmen sind haeufig und berechtigt**: eine Markenvorschau, ein Diagramm mit eigener Farbskala, ein Bild in Bildpunkten. Die gehoeren quittiert. Der Rest gehoert an `var(--token)`. Wie L-32, wo dieselbe Aufraeumarbeit 39 Vorkommen in 21 Dateien betraf.

*Beleg:* `kompagnon/frontend/src/pages/LeadProfile.jsx — Zeile 761, 765, 818, 829, 832, 840, 844, 851`

*Kennung:* `theme/farbe-statt-token/kompagnon/frontend/src/pages/LeadProfile.jsx` — in `docs/durchlauf/quittiert.json` eintragen, um den Befund dauerhaft abzuraeumen.

**SectionDetailPanel.jsx: 35 feste Farbwerte, die beim Umschalten nicht mitgehen** · Vorschlag P2

Der Dunkelmodus tauscht **Tokens**, keine Zeichenketten. Eine Farbe, die als `'#…'` im Bauteil steht, bleibt beim Umschalten stehen: Die Flaeche wird dunkel, die Schrift bleibt fast schwarz. Das ist nicht nur haesslich, es ist unlesbar — und im Hellmodus faellt es nie auf. **Ausnahmen sind haeufig und berechtigt**: eine Markenvorschau, ein Diagramm mit eigener Farbskala, ein Bild in Bildpunkten. Die gehoeren quittiert. Der Rest gehoert an `var(--token)`. Wie L-32, wo dieselbe Aufraeumarbeit 39 Vorkommen in 21 Dateien betraf.

*Beleg:* `kompagnon/frontend/src/components/wireframe/SectionDetailPanel.jsx — Zeile 188, 194, 231, 235, 238, 244, 261, 262`

*Kennung:* `theme/farbe-statt-token/kompagnon/frontend/src/components/wireframe/SectionDetailPanel.jsx` — in `docs/durchlauf/quittiert.json` eintragen, um den Befund dauerhaft abzuraeumen.

**TemplateLibrary.jsx: 30 feste Farbwerte, die beim Umschalten nicht mitgehen** · Vorschlag P2

Der Dunkelmodus tauscht **Tokens**, keine Zeichenketten. Eine Farbe, die als `'#…'` im Bauteil steht, bleibt beim Umschalten stehen: Die Flaeche wird dunkel, die Schrift bleibt fast schwarz. Das ist nicht nur haesslich, es ist unlesbar — und im Hellmodus faellt es nie auf. **Ausnahmen sind haeufig und berechtigt**: eine Markenvorschau, ein Diagramm mit eigener Farbskala, ein Bild in Bildpunkten. Die gehoeren quittiert. Der Rest gehoert an `var(--token)`. Wie L-32, wo dieselbe Aufraeumarbeit 39 Vorkommen in 21 Dateien betraf.

*Beleg:* `kompagnon/frontend/src/pages/TemplateLibrary.jsx — Zeile 145, 146, 147, 148, 149, 150, 256, 257`

*Kennung:* `theme/farbe-statt-token/kompagnon/frontend/src/pages/TemplateLibrary.jsx` — in `docs/durchlauf/quittiert.json` eintragen, um den Befund dauerhaft abzuraeumen.

**2 Messungen am gerenderten Bild sind in diesem Lauf nicht gefahren worden** · Vorschlag P2

Kontrast und Schriftgröße entscheiden über Lesbarkeit, und beide lassen sich am Quelltext nicht beantworten — eine Farbe kann als Token bestehen und auf der Seite trotzdem auf dem falschen Grund landen. **Schriftgrößen am gerenderten Text**: misst gewichtet nach Zeichenmenge, Grenze 12 px wie Lighthouse — die belastbare Fassung der Lesbarkeitsfrage. **Kontrast und Fokusreihenfolge**: misst, was tatsächlich übereinanderliegt, statt Token-Paare zu rechnen; erkennt Text auf Bild und Verlauf aus den Bildpunkten. Beide brauchen ein laufendes Frontend und eine Anmeldung; sie dauern Minuten. **Dieser Eintrag verschwindet, sobald sie gelaufen sind** — er steht hier, damit ein Bericht ohne sie nicht sauberer aussieht als die Lage ist. Und seit dem 04.09. gilt: Beide gehören in **beiden** Themes gefahren, hell und dunkel zählen gleich.

*Beleg:* `Schriftgrößen am gerenderten Text (`tools/schriftgroessen_messen.py`) · Kontrast und Fokusreihenfolge (`tools/bedienbarkeit_messen.py`)`

*Kennung:* `am-bild/nicht-gemessen` — in `docs/durchlauf/quittiert.json` eintragen, um den Befund dauerhaft abzuraeumen.

**Sprunglink und Themenschalter erreichen im Dunkelmodus nur 1,86 bzw. 2,06:1** · Vorschlag P2

Im Dunkelsatz wird `--kc-mid` auf ein helles Cyan (#40c4df) gehoben, damit es auf dunklem Grund leuchtet. Weiße Schrift darauf hat dann fast keinen Abstand mehr. Betroffen sind der Sprunglink „Zum Inhalt springen" — ausgerechnet die Hilfe für Tastaturnutzer — und die Sonnenmarke des Themenschalters. Auf hellem Cyan gehört dunkle Schrift; `--text-on-brand` steht im Dunkelsatz bereits auf #0f1c20 und wäre der richtige Wert.

*Beleg:* `Weiß rgb(255,255,255) bzw. rgb(240,244,245) auf --kc-mid rgb(64,196,223) — 2.06 bei 14 px (Schwelle 4,5) und 1.86 bei 12 px. Gemessen am 04.09.2026 auf /app/dashboard, Theme dark.`

*Kennung:* `kontrast/dark/sprunglink-und-schalter` — in `docs/durchlauf/quittiert.json` eintragen, um den Befund dauerhaft abzuraeumen.

### Konsistenz und Bauwerk

**`botocore` wird importiert, steht in keiner Anforderungsdatei** · Vorschlag P1

`requirements.txt` sagt in ihrem eigenen Kopf, sie enthalte „die vollstaendig aufgeloesten Versionen **inklusive aller transitiven Pakete**". Ein importiertes Modul, das dort fehlt, widerspricht dem — und die Datei gibt es genau deshalb, weil zwei Deploys desselben Commits sonst unterschiedliche Software ergeben. Der laufende Dienst hat das Paket — er wurde damit gebaut. Der **naechste** Neuaufbau hat es nicht. Genau so ist L-57 entstanden: Der Buildbefehl rief `playwright` auf, das in keiner Anforderungsdatei stand; aufgefallen ist es erst beim Umzug, als ein neuer Dienst nach 28 Sekunden scheiterte.

*Beleg:* `kompagnon/backend/services/produktablage.py:84`

*Kennung:* `import-ohne-eintrag/botocore` — in `docs/durchlauf/quittiert.json` eintragen, um den Befund dauerhaft abzuraeumen.

**in 23 Tagen: Perplexity schaltet die alte Anfrageform ab (L-81)** · Vorschlag P1

Ab dem Stichtag liefert die KI-Sichtbarkeitsmessung fuer Perplexity keine Antwort mehr. Der Test scheitert mit der Arbeitsanweisung im Text.

*Beleg:* `2026-09-27 — Herstellerankuendigung; tests/test_perplexity_altform_termin.py wird ab dem Stichtag rot`

*Kennung:* `termin/perplexity-altform` — in `docs/durchlauf/quittiert.json` eintragen, um den Befund dauerhaft abzuraeumen.

**kompagnon/backend/migrations_runtime.py hat 1882 Zeilen (Grenze 800)** · Vorschlag P3

Ueber der doppelten Grenze gemeldet, damit die Liste nicht von Grenzfaellen lebt. Gezaehlt wird mit derselben Methode wie in `tools/grosse-dateien.py` (L-25) — Zeilen, nicht Zeichen.

*Beleg:* `wc -l kompagnon/backend/migrations_runtime.py → 1882`

*Kennung:* `dateigroesse/kompagnon/backend/migrations_runtime.py` — in `docs/durchlauf/quittiert.json` eintragen, um den Befund dauerhaft abzuraeumen.

**kompagnon/frontend/src/data/templates_zusatz.js hat 3122 Zeilen (Grenze 800)** · Vorschlag P3

Ueber der doppelten Grenze gemeldet, damit die Liste nicht von Grenzfaellen lebt. Gezaehlt wird mit derselben Methode wie in `tools/grosse-dateien.py` (L-25) — Zeilen, nicht Zeichen.

*Beleg:* `wc -l kompagnon/frontend/src/data/templates_zusatz.js → 3122`

*Kennung:* `dateigroesse/kompagnon/frontend/src/data/templates_zusatz.js` — in `docs/durchlauf/quittiert.json` eintragen, um den Befund dauerhaft abzuraeumen.

**kompagnon/frontend/src/pages/LeadProfile.jsx hat 1686 Zeilen (Grenze 800)** · Vorschlag P3

Ueber der doppelten Grenze gemeldet, damit die Liste nicht von Grenzfaellen lebt. Gezaehlt wird mit derselben Methode wie in `tools/grosse-dateien.py` (L-25) — Zeilen, nicht Zeichen.

*Beleg:* `wc -l kompagnon/frontend/src/pages/LeadProfile.jsx → 1686`

*Kennung:* `dateigroesse/kompagnon/frontend/src/pages/LeadProfile.jsx` — in `docs/durchlauf/quittiert.json` eintragen, um den Befund dauerhaft abzuraeumen.

## Was keine Maschine beantwortet

5 Fragen gefuehrt, **4 davon faellig**. Wer eine
beantwortet, traegt das Datum bei `zuletzt` ein — dann verschwindet sie
bis zum naechsten Mal.

**Misst der Website-Standard noch, was er verspricht? Ein Lauf gegen die eingefrorene Referenzseite, Sollwerte verglichen.** · noch nie beantwortet

L-107, L-150 bis L-155 waren alle von dieser Art: Die Erhebung lief, die Zahl entstand, und sie bedeutete etwas anderes als der Text daneben. Kein Skript merkt das — es braucht jemanden, der Messwert und Versprechen nebeneinanderlegt.

**Sind die sechs Hauptbildschirme noch selbsterklaerend? Krug-Durchgang nach docs/ux-soll-ist-kas.md § 2.** · noch nie beantwortet

Bedienbarkeit ist ein Urteil, keine Messung. Kontrast und Schriftgroesse sind vermessen; ob jemand versteht, was er sieht, nicht.

**Sind Lizenzlage (Studio-SDK, node_modules) und Rechtstexte (DDG, USt.) noch tragfaehig?** · noch nie beantwortet

L-148, L-149, L-122, L-118. Keine Maschine beurteilt eine Lizenz. Maschinell moeglich ist hoechstens ein Abgleich der Lizenzfelder gegen eine Freigabeliste — die Frage danach bleibt menschlich.

**Laesst sich eine Sicherung wirklich zurueckspielen? Einmal ausprobiert, nicht nur geprueft, dass sie existiert.** · noch nie beantwortet

Eine Sicherung, die noch nie zurueckgespielt wurde, ist eine Annahme. Der Standard fragt Kunden genau das; das eigene Haus sollte die Antwort kennen.


## Was dieser Lauf nicht gesehen hat

* **Ob eine Anzeige stimmt.** Der Lauf sieht, dass ein Feld gelesen wird,
  nicht, ob der richtige Wert darin steht.
* **Gestaltung.** Farben werden gegen die Vorgabe gemessen; ob eine
  Oberflaeche gut ist, steht in `docs/ux-soll-ist-kas.md`.
* **Verhaltensaenderungen fremder Bibliotheken.** Ein Import gelingt und
  liefert trotzdem etwas anderes als frueher — L-140 war so. Dagegen
  hilft nur ein Test, der das Verhalten festhaelt.
