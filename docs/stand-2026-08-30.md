# KOMPAGNON — Stand vom 2026-08-30

> Tagesbericht. Der Auftrag war: **die Punkte abarbeiten, die ohne David
> gehen** — aus der Lage vom Morgen waren das L-81, L-95, L-103, L-105,
> L-106, L-146, L-25 und der Rest von L-17.
>
> Vier dieser acht sind beim Nachmessen weggefallen, bevor eine Zeile Code
> entstand. Was blieb, waren zwei — und beide haben mehr gefunden, als auf
> ihnen stand.

---

## Das Muster des Tages

**Vier Wächter waren grün, weil sie nicht hingesehen haben.**

| Wächter | Sagte | Meinte |
|---|---|---|
| `tokenKontrastPaare.test.js` | „alle Paare bestehen" | „die Hälfte der Paare habe ich übersprungen" |
| die sechs Alpha-Token | „Schatten vorhanden" | ein Ringschluss, der zu nichts auflöst |
| die Paarliste selbst | „`--text-brand` ist geprüft" | der Token stand nie darin |
| `tools/unaufgerufene-routen.py` (Vortage) | dieselbe Familie | siehe L-105 |

Und dreimal ist mir derselbe Fehler in der **eigenen** Messung passiert: Ein
Wächter zählte den Kommentar, der den Fehler beschreibt. Ein zweiter zählte
das `<a` des Sprunglinks als Element **vor** dem Sprunglink. Ein dritter
hätte fast einen `:focus-visible`-Effekt als Befund gemeldet.

**Die Lehre steht dreifach da und ist jedes Mal dieselbe:** Wer über den
Rohtext sucht, misst den Text und nicht die Sache. Alle drei sind vor dem
Melden aufgefallen, zwei davon durch die Tests, die ich gerade schrieb.

---

## 1. Vier Punkte fielen beim Nachmessen weg

Bevor irgendetwas gebaut wurde, wurden alle acht am Gegenstand geprüft. Das
Ergebnis änderte den Tagesplan:

* **L-81** (Perplexitys alte Schnittstelle) — **nichts zu tun bis zum
  27.09.2026.** Angebunden ist längst die neue Agent-API; der zweite
  Lesezweig stirbt erst mit dem Stichtag. `tests/test_perplexity_altform_termin.py`
  wird an diesem Tag von selbst rot und trägt die Arbeitsanweisung im Text.
  Meine Empfehlung von heute Morgen, ihn „zuerst" zu machen, war falsch —
  sie kam aus dem Titel, nicht aus dem Eintrag.
* **L-95** (toter Frontend-Code) — gemessen **174 Zeilen** statt 1.918, und
  171 davon sind Prüfwerkzeug, das dort hingehört. Übrig bleibt
  `allTemplates.js` mit drei Zeilen, und die hängen an **45 Website-Vorlagen**
  für genau unsere Zielbranchen. Das ist Davids Entscheidung (anschließen,
  in die Datenbank übernehmen oder verwerfen), keine Aufräumarbeit.
* **L-103** (Places-Schlüssel in der URL) — braucht Zugang zum Google-Projekt.
* **L-106** / **L-146** — beide brauchen eine Entscheidung über den
  Datenbestand beziehungsweise einen Eingriff in die Produktivdatenbank.

Damit blieben **L-25** und **L-17**.

---

## 2. L-25 — die Zahl war gestiegen, nicht gefallen

Im Lagebild standen „3 im Backend". Gemessen waren es **7**: `main.py` war
seit dem Herausziehen der Migrationen am 22.08. auf 1.221 Zeilen
zurückgewachsen, dazu vier weitere über die Grenze gerutscht.

**Jeder Schnitt ging nach Zuständigkeit, keiner nach Größe:**

| Datei | vorher | nachher | was ausgezogen ist |
|---|---:|---:|---|
| `main.py` | 1.221 | 667 | `startphase.py` (was einmal beim Start schreibt), `routers/betriebszustand.py` (`/health`, `/info`, `/`, Scheduler) |
| `services/audit_criteria.py` | 998 | 363 | `audit_kriterium.py` (die Formen), `audit_katalog.py` (der Inhalt) |
| `routers/projects.py` | 887 | 737 | `projects_fortschritt.py` (Zeiten, Marge, Checkliste) |
| `database.py` | 845 | 689 | `modelle_konten.py`, `modelle_meldungen.py` |
| `routers/leads.py` | 805 | 726 | `POST /enrich/all` → `leads_anreicherung.py`, `leads_briefing.py` |

**Der Katalog ging in drei Teile, nicht in zwei**, weil dort drei Dinge lagen:
was ein Kriterium *ist*, *was* geprüft wird, und *wie* daraus eine Note wird.
Die Formen mussten zuerst gehen — beide anderen brauchen sie, und stünden sie
beim Inhalt, müsste die Rechnung den Katalog importieren, um eine Form zu
kennen. Ein Ringschluss wäre dann nur eine Frage der Zeit.

**502 Endpunkte vorher, 502 nachher**, Pfad für Pfad und Methode für Methode
über `openapi()` verglichen — nicht über `app.routes`, das kennt 88 davon.

### Was die Tests gefunden haben, was ich nicht gesehen hätte

* **Zwei Tests lesen die Produktvorlage aus dem Quelltext**, einer greift nach
  `_ZAHLUNGSWERTE`. Alle drei wurden rot, als die Blöcke umzogen. Genau dafür
  sind sie da.
* **Ein vierter kam aus dem Frontend:** `stufenGleichstand.test.js` liest
  `LEVELS` aus der Backend-Quelle. Meine Suche nach Aufrufern hatte nur `.py`
  durchsucht.
* **Die Buch-Ausgabe lädt den Katalog über den Dateipfad**, absichtlich —
  `services/__init__.py` zöge Datenbankmodule mit. Ein Pfadimport kennt keinen
  Paketnamen, und der neue `services.audit_katalog`-Import scheiterte. Der
  Lader stellt jetzt ein leeres Paket dieses Namens in `sys.modules`, dessen
  `__path__` auf den Ordner zeigt: Der übliche Mechanismus findet die
  Geschwister, `__init__.py` bleibt ungelesen.
* **Und ein Wächter fing eine Verschlechterung, die ich eingebaut hatte.**
  `leads.py` hängt `require_innendienst` am **Router**; die Funktion selbst
  trägt nur `require_any_auth`. Wer sie herauslöst und den neuen Router ohne
  diese Abhängigkeit anlegt, macht aus „Innendienst" still „irgendwer ist
  angemeldet" — ein Kunde hätte die Briefing-Vorschläge **jedes** Betriebs
  abrufen können. `test_zugriffsschutz_bestand` meldete 59 statt 58.
  Beim Lesen fällt das nicht auf, weil an der Funktion nichts fehlt.

**Stand: 2 statt 7 im Backend.** `migrations_runtime.py` bleibt bewusst ganz
(ein chronologisches Journal). `pdf_generator.py` bleibt ebenfalls stehen:
Zwei Drittel davon sind **eine** Funktion mit 574 Zeilen, und der Bericht geht
an Kunden. Der Vergleichstest dafür (`test_pdf_unveraendert.py`) steht seit
dem 22.08. bereit — der Schnitt ist der nächste Schritt und kein Nebenbei.

Im Frontend unverändert 15 Dateien über der Grenze.

---

## 3. L-17 — der Fokusring war seit dem 09.05. weg, und niemand konnte es sehen

Von den vier Klassen waren zwei geschlossen und eine vermessen. Für **Fokus**
und **Kontrast** gab es bis heute keine Zahl, sondern einen Eindruck.

`tools/bedienbarkeit_messen.py` misst beides im Browser, angemeldet, auf
denselben sechs Seiten wie die Schriftgrößenmessung — und zählt **Zeichen**
statt Fundstellen, aus demselben Grund wie dort: Ein Verstoß in jeder
Tabellenzeile wiegt anders als einer, der dreimal vorkommt.

### Der Fund

**11 von 163 erreichbaren Elementen zeigten beim Fokussieren nichts** — sechs
Eingabefelder, fünf Auswahllisten. Vor dem Melden am Gegenstand nachgesehen,
weil `:focus-visible` nicht bei jeder Elementart auf einen Programm-Fokus
anspricht. Bei diesen tut es das, und `outline` stand **vorher wie nachher**
auf `0px`. Kein Messartefakt.

Die Ursache steht in `tokens.css`, seit dem 09.05.2026:

```css
--kc-mid-a-12: var(--kc-mid-a-12);
```

**Sechs Token definierten sich selbst.** Ein Ringschluss ist nach CSS
*invalid at computed-value time*: Das Token löst zu **nichts** auf, und jede
Deklaration, die es benutzt, fällt ersatzlos weg. Im Browser gemessen kamen
alle sechs als `""` zurück. Betroffen: **41 Verwendungen in 17 Dateien** —
darunter

```css
input:focus, select:focus, textarea:focus {
  outline: none;
  box-shadow: 0 0 0 3px var(--kc-mid-a-12);
}
```

also die **einzige** Fokusmarke, die ein Formularfeld im ganzen Werkzeug hat.

**Warum es niemandem auffiel:** Ein Schatten, der wegfällt, sieht aus wie ein
Schatten, den niemand vorgesehen hat. Nichts wird rot, nichts bricht — die
Oberfläche ist nur stiller, als sie gebaut wurde.

Nach der Reparatur: **11 → 0**.

### Ein Sprunglink, den es nie gab

Dieselbe Messung zeigte **38 Tabstopps vor dem Inhalt** auf `/app/betriebe`,
auf jeder Seite wieder. Ein Sprunglink (WCAG 2.4.1, Stufe A) existierte im
ganzen Werkzeug nicht.

Jetzt steht er zuerst im Baum, unsichtbar bis zum Fokus, Ziel
`<main id="inhalt" tabIndex={-1}>`. **Der `tabIndex` gehört dazu:** Ohne ihn
scrollt der Browser zum Ziel, setzt aber den Fokus nicht — der nächste
Tabulator führt zurück an den Anfang, und der Sprung *sieht* nur aus, als
hätte er gewirkt. Mit der Tastatur am laufenden Werkzeug nachgemessen: Tab,
Enter, Tab landet auf dem ersten Bedienelement **im Inhalt**. 38 → 1.

### Beim Kontrast war der vorhandene Wächter das Fundstück

`tokenKontrastPaare.test.js` prüft `--brand-primary-mid` gegen beide hellen
Gründe und war **grün**. Der Browser sagte **3.69**. Beides kann nicht stimmen.

Der Test las nur `--name: #rrggbb;`. In `:root` ist fast jedes Markentoken ein
**Verweis** — `--brand-primary-mid: var(--kc-mid);` —, und solche Zeilen
fielen durch das Muster. Die Schleife darunter überspringt jedes Paar, dessen
Token fehlt, „weil es erbt". Der **helle Modus war für diese Paare nie
geprüft**, und der Test sagte „in Ordnung" und meinte „nicht angesehen". Seine
eigene Vollständigkeitsprüfung ließ zwei stillschweigend fehlende Paare zu —
und eines davon war der Befund.

Er löst jetzt Verweise auf, wie der Browser es tut, und wird rot auf genau die
Werte, die die Messung nannte — auf die zweite Nachkommastelle, aus zwei
Messungen, die einander nicht kennen.

**Behoben wurde die Rolle, nicht die Marke.** `--brand-primary-mid` ist im
Hellmodus `#00778F`: derselbe Farbton, dieselbe Sättigung (190°, 100 %), der
**hellste** Wert dieser Reihe, der beide Gründe besteht (4.99 / 4.70).
`--kc-mid` bleibt Pantone 3135 und unangetastet — als Fläche, Rand und Füllung
gilt die Textschwelle nicht.

Zwei Nachbarn kamen mit: `--text-brand` heißt „text", wird als Linkfarbe
benutzt und stand **nicht** in der Paarliste; und die Dashboard-Zeile „Alle →"
trug noch das Primitiv statt des semantischen Tokens — eine Stelle, die der
Mid→Dark-Wechsel vom 08.05.2026 übersehen hat.

**Gemessen: 63 von 3.038 Zeichen unter AA → 8 von 3.152.** 2,1 % → 0,3 %.

---

## 4. Drei eigene Messfehler, alle vor dem Melden gefangen

* Der neue Token-Wächter zählte **den Kommentar**, der den Fehler beschreibt.
  Kommentare fallen jetzt vorher heraus.
* Der Sprunglink-Wächter zählte das `<a` **des Sprunglinks** als Element davor.
  Er misst jetzt ab dem Tag, nicht ab dem Attribut.
* Die Fokusmessung hätte beinahe einen `:focus-visible`-Effekt als Befund
  gemeldet. Am Gegenstand gegengeprüft, bevor die Zahl in den Bericht ging.

Zwei der drei haben die Tests gefunden, die ich in derselben Stunde geschrieben
habe. Das ist kein Zufall, sondern ihr Zweck.

---

## Was David entscheiden muss

| # | Frage | Löst |
|---|---|---|
| 1 | Die **Statusfarben-Palette**: Bernstein 2,88 · Cyan 3,33 · Smaragd 3,40 · Grün 2,98 auf `--surface`. Acht Ziffern fallen dadurch unter AA. Das ist die Tool-CI, kein Fehler. | Rest von L-17, Kontrast |
| 2 | **11 px → 12 px** (Messung vom 29.08.): eine Größe trägt ein Drittel des Textes. | Rest von L-17, Lesbarkeit |
| 3 | Die **45 Website-Vorlagen** in `data/templates*.js` — anschließen, in die Datenbank übernehmen oder verwerfen? | L-95 |
| 4 | `usercards` (49 Zeilen) und `customers` (0): Zusammenlegung zu Ende bringen oder Tabelle samt Routen fallen lassen? | L-106, Teil von L-105 |
| 5 | Drei tote Tabellen produktiv fallen lassen (`schema_migrations` zuerst). | L-146 |

Dazu unverändert die Liste vom 29.08.: die drei KI-Schlüssel, die Stripe- und
R2-Werte, AGB und Widerrufsbelehrung von der Kanzlei, die Abo-Achse und eine
Testmail.

---

## 5. Abends: L-25 zu Ende, soweit es verantwortbar war

**Backend 7 → 1, Frontend 15 → 4.**

`pdf_generator.py` war die letzte Backend-Datei über der Grenze und zu zwei
Dritteln **eine** Funktion. Sie ist jetzt drei Teile — womit gebaut wird, was
auf jeder Seite steht, und der Zusammenbau. Möglich war das nur, weil die
Gegenprobe seit dem 22.08. bereitlag; der Eintrag hatte den Umbau bis dahin
bewusst aufgeschoben, weil der Bericht an Kunden geht.

Im Frontend elf Dateien, immer nach demselben Muster: Unterkomponenten, die
schon eigene Funktionen waren, in einen Ordner nach ihrem Gegenstand — und
**das Gemeinsame zuerst**, sonst importiert eine Komponente ihre Nachbarin.

| Datei | vorher | nachher |
|---|---:|---:|
| `SitemapViewV2.jsx` | 2.210 | 575 |
| `WireframeView.jsx` | 1.719 | 724 |
| `StyleGuideView.jsx` | 1.560 | 813 |
| `ComponentLibrary.jsx` | 1.264 | 585 |
| `BriefingWizard.jsx` | 1.261 | 738 |
| `AppLayout.jsx` | 1.183 | 486 |
| `AnalyseCentrale.jsx` | 1.100 | 791 |
| `AuditReport.jsx` | 1.025 | 656 |
| `CustomerPortal.jsx` | 986 | 687 |
| `LeistungsseitenWizard.jsx` | 970 | 654 |
| `AcademyAdminCourse.jsx` | 936 | 553 |
| `CustomerDetail.jsx` | 2.512 | 1.413 |
| `LeadProfile.jsx` | 2.747 | 2.012 |

### Sieben Wächter haben Umzüge gemeldet, die beim Lesen unsichtbar sind

Zwei Leser der Produktvorlage, `_ZAHLUNGSWERTE`, `stufenGleichstand` (liest
die Stufentabelle aus der Berichtsquelle), `akademieWege` (fand **null**
Bearbeiten-Knöpfe, weil sie aus `pages/` ausgezogen waren), `paketpreise` und
`feldName` — deren Ausnahmelisten müssen mit ihrem Gegenstand mitwandern,
sonst decken sie eine Datei, die die Sache nicht mehr enthält.

Und `seitenTitel` hat mir gesagt, dass ich Komponenten unter `pages/` abgelegt
hatte. Er hatte über mehr recht als über Überschriften.

### Zwei Reiter ließen sich nicht herauslösen

`qrcode` ist eine sofort aufgerufene Funktion, `design` trägt ein
`createPortal` im selben Ausdruck. Mein Schnitt hat beide zerrissen; der Build
meldete „Unterminated JSX contents" am Dateiende — so sieht ein verlorenes
schließendes Tag für einen Parser aus. Beide stehen wieder an ihrem Platz.

**Ein Muster, das für zwölf Reiter hält, hält nicht für alle vierzehn** — und
das sieht man der Marke nicht an.

---

## 6. Abends: L-17, die Tastaturklasse eingeordnet statt gezählt

Die 44 Stellen der Ratsche wurden einzeln angesehen. **34 davon sind
Modal-Hintergründe**, und für die ist das Heilmittel der Ratsche das falsche:
`role="button"` auf einer Überlagerung behauptet eine Schaltfläche, wo keine
ist, und ein Screenreader liest sie als solche vor.

Der wirkliche Mangel dort ist ein anderer und lässt sich messen: **26 Dateien
mit einer bildschirmfüllenden Überlagerung hatten keinen Escape-Weg.** Wer ein
Modal öffnete, kam mit der Tastatur nicht wieder heraus. Jetzt sechs, jede mit
Grund.

**Das Rendern fand dabei einen Fehler, den 558 Tests nicht finden konnten:**
Der Hook las Zustand, den `useState` weiter unten anlegt —
`Cannot access 'showModal' before initialization`, und `CustomerDetail`
renderte **gar nicht**. Der Build übersetzt das klaglos, Jest rendert die
Seite nicht. Nur das Öffnen von `/app/customers/1` im Browser hat es gesagt.

Die Ratsche steht bei 42, und ihr Kopftext sagt jetzt, dass diese Zahl ein
**Muster** misst und keinen Mangel mehr.

---

## Ein Fund, der beim Testen abfiel — älter als heute

`/app/betriebe/12` — ein Betrieb, den es lokal nicht gibt — rendert eine
**leere Seite** und wirft `Cannot read properties of undefined (reading
'status')`. Gegen den unveränderten Stand geprüft: dort ebenso. Nicht behoben,
weil die Frage, was ein fehlender Datensatz zeigen soll, eine Produktfrage
ist.

---

## Nächster Schritt

Die vier Dateien, die über der Grenze bleiben, sind es aus vier verschiedenen
Gründen: `templates_zusatz.js` ist eine Datenliste (bewusst ganz),
`LeadProfile.jsx` (2.012) und `GeoOptimizerStep.jsx` (813) sind je **eine**
Funktion, `CustomerDetail.jsx` (1.413) ist die nächste, an der dasselbe
Verfahren greift.

Bei `LeadProfile` liegen noch zwölf Reiter in der Rückgabe; die zwei
herausgelösten zeigen, dass es geht — die zwei zurückgeholten zeigen, dass
jeder Block vorher gelesen werden will.
