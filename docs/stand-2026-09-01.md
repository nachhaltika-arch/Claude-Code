# KOMPAGNON — Stand vom 2026-09-01

> Tagesbericht. Sechs Aufträge: „mach mit P2 weiter" · „mach L-105" ·
> „triff sinnvolle Entscheidungen für mich" (zweimal) · „per Rechnung" ·
> „dokumentieren und Feierabend".
>
> **16 Commits, neun CI-Läufe, einer davon rot.**
>
> Die Lehre des Vormittags: **Wer nur nach der erwarteten Form sucht, findet
> die erwartete Zahl — und hält sie für die Sache.** Der Abend hat sie
> dreimal bestätigt, und beim letzten Mal war der Fehler meiner.

---

## Das Muster des Tages

Dreimal derselbe Ablauf: Die Suche fand genau das, was gesucht war, war
danach zufrieden — und daneben stand jedes Mal noch etwas.

| Gesucht | Gefunden | Was daneben stand |
|---|---|---|
| `fontSize: <Zahl>` unter 12 px | 112 Stellen | `'0.65rem'` und `text-[11px]` — zwei andere Schreibformen, eine davon in **Kundenseiten** |
| „Wer liest die Druckwarteschlange?" | niemand | sie stand voller **unbezahlter** Bestellungen |
| „Ist CORS konfigurierbar?" | ja, längst | der Netlify-Ausdruck erlaubte mehr, als er sollte |

In allen drei Fällen war der zweite Fund **schwerer** als der erste.

---

## 1 · L-17 — die letzte Größenklasse, und zwei Formen dahinter

Die 112 Stellen mit 9 bzw. 8 px sind auf 12 px. Damit ist die Stufenfolge zu
Ende, die am 30.08. bei 11 px begann und am 31.08. bei 10 px weiterging: **Im
Quelltext des Werkzeugs steht keine Schriftgröße mehr unter 12 px.**

Ersetzt mit Grenzprüfung, weil die Hebung am 31.08. ohne sie aus `11.5` ein
`12.5` gemacht hatte. Gegengeprüft: `13.5` steht unverändert.

**Zwei Fundstellen kamen erst dazu, als die erwartete Form erschöpft war:**

* `AlertBanner.jsx` setzte `fontSize: '0.65rem'` — 10,4 px — auf ein Abzeichen,
  dessen Klasse `.kc-badge` bereits 12 px führt. Die Inline-Angabe drückte den
  Wert also **aktiv** herunter. Sie ist entfernt statt auf eine neue Zahl
  gesetzt; die Klasse hat den richtigen Wert schon.
* Zwei Bausteine der Bibliothek trugen `text-[11px]`. **Diese gehen in
  Kundenseiten, nicht in unser Werkzeug** — und es ist ausgerechnet die
  Kleingedruckte unter dem Formular: einmal der Verweis auf die
  Datenschutzerklärung, einmal die Zusage, dass jeder Termin schriftlich
  bestätigt wird. Unser eigener Prüfkatalog misst bei Kunden die Lesbarkeit;
  wir hätten ausgeliefert, was wir selbst abwerten.

**Ein Wächter hält es jetzt** (`tests/test_schriftgroessen.py`). Drei Hebungen
in drei Tagen waren Handarbeit über Dutzende Dateien, und jede war in dem
Moment rückgängig gemacht, in dem die nächste Komponente wieder `fontSize: 9`
schreibt. Er kennt alle drei Schreibformen und rechnet `rem` in `px` um.

Er sagt ausdrücklich, was er **nicht** kann: Er zählt Stilangaben im Quelltext,
nicht gerenderten Text. Lighthouse gewichtet nach Zeichenmenge, und diese 112
Stellen kommen auf den sechs gemessenen Seiten gar nicht zur Anzeige — der
Inhaltsbereich stand schon vorher bei 0 %. Die Zahl, auf die es ankommt, misst
weiterhin `tools/schriftgroessen_messen.py` am laufenden Werkzeug.

Zwei Gegenproben daneben, weil eine Abwesenheits-Zusicherung allein immer grün
ist — und nachgewiesen, dass der Wächter rot wird: eine Stelle testweise auf 9
zurückgedreht, er nennt Datei, Zeile und Wert.

**Offen an L-17 bleibt nur, was aus zwei Farben nicht entscheidbar ist:** die
20 % Text auf Bild oder Verlauf und sechs Modale mit Grund.

---

## 2 · BUCH-07 — die Warteschlange bekommt einen Leser

`fulfillment_status` wurde beim Bestellen auf `queued` gesetzt und danach von
niemandem gelesen. Keine Ansicht, kein Export, kein Weg für eine
Sendungsnummer; `fulfillment_exported_at` und `tracking_number` waren Spalten,
die nie jemand beschrieb.

**Der Fund beim Anschließen ist teurer als die Lücke selbst.**
`_bestellung_anlegen` setzte `queued` bereits beim **Anlegen**, während
`payment_status` noch auf `pending` stand. Jede abgebrochene Kasse lag damit in
der Druckwarteschlange. Wer diese Liste bei BoD aufgegeben hätte, hätte
gedruckt und an eine Lieferanschrift versandt, zu der nie eine Zahlung kam.

> **Eine ungelesene Liste verbirgt ihre eigenen Fehler.**

Behoben an der Ursache: Druckbestellungen stehen bis zur Bestätigung durch
Stripe auf `awaiting_payment`. Der Export prüft `payment_status` trotzdem ein
zweites Mal — ältere Zeilen tragen den falschen Status weiter, und ein Status
ist eine Behauptung, während die Zahlung eine Tatsache ist.

**Drei Endpunkte** (`routers/buch_versand.py`), alle hinter `require_admin`,
dazu `pages/BuchBestellungen.jsx` unter `/app/buchbestellungen`.

**Der Export ist ein POST, obwohl der Auftrag GET vorsah.** Er setzt die Zeilen
auf `exported`; ein Vorauslader, ein Doppelklick oder ein Neuladen leerte die
Warteschlange, ohne dass jemand die Datei gesehen hat. Es kostet auch nichts —
der Endpunkt braucht einen Anmeldekopf, den ein `<a href>` gar nicht
mitschicken kann.

**Zu BoD entsteht ausdrücklich keine Verbindung.** Es gibt dort keine
öffentliche Bestell-API; eine Automatik, die im Betrieb still fehlschlägt, wäre
schlechter als die Handarbeit.

**Ein bestehender Wächter hat mich korrigiert.** `test_frontend_adressen` wurde
rot: Der erste Entwurf schrieb `…/orders${abfrage ? `?${abfrage}` : ''}` in den
fetch-Aufruf, und der Vergleich zählt Abschnitt für Abschnitt. Der Wächter
hatte recht, und lesbarer ist es auch.

### Zwei Fragen für David

| Frage | Warum sie jetzt kommt |
|---|---|
| **Anrede erheben?** | Die CSV-Spalte bleibt leer — die Kasse fragt sie nie ab. Wer sie füllen will, erhebt sie zuerst beim Kauf. |
| **Mehrere Exemplare?** | `Menge` ist immer 1, weil Stripe fest `"quantity": 1` bekommt. Fünf Bücher für ein Team wären heute fünf Bestellungen. |

Beides ist eine Produktfrage, keine Lücke im Code.

---

## 3 · BUCH-09 — der Fehler, der nirgends ein Protokoll erzeugt

Fehlt die CORS-Erlaubnis, hält der Browser die Anfrage an, **bevor sie
ankommt**:

| Wo man sucht | Was man sieht |
|---|---|
| Render-Log | nichts |
| Stripe | nichts |
| Datenbank | nichts |
| Netlify-Log | nichts |
| Browserkonsole | `blocked by CORS policy` |

**Die Bestandsaufnahme erledigte den Auftrag zur Hälfte.** Die Herkünfte sind
seit Langem konfigurierbar, `allow_credentials=True` steht korrekt neben
ausdrücklichen Herkünften statt neben `"*"`, und es gibt nur **eine**
Middleware — die Reihenfolgefrage aus Schritt 3 stellt sich gar nicht.

Der Auftrag nennt die Variable `ALLOWED_ORIGINS`; gesetzt ist an den Diensten
`CORS_ALLOWED_ORIGINS`. **Umbenannt wird nicht** — das machte eine gesetzte
Variable am laufenden Dienst wirkungslos.

Gebaut wurde, was fehlte:

* **eine gemeinsame Quelle** (`cors_herkuenfte.py`), weil die Liste einen
  zweiten Leser bekam und zwei Stellen, die sie je eigen zusammenbauen, zwei
  Wahrheiten sind;
* **eine Zeile beim Start** — `CORS erlaubte Origins: [...]`;
* **`GET /api/health/cors`**, ohne Anmeldung, weil wer eine *fremde*
  Landingpage prüft, dort kein Token hat;
* **`scripts/check-cors.sh`** für Preflight und Auskunft in einem Aufruf.

**Der eigene Test fand einen Bestandsfehler.** `allow_origin_regex` stand auf
`https://.*\.netlify\.app`, und `.*` schließt den Schrägstrich ein:
`https://fremde.example/pfad.netlify.app` passte darauf. Über den Browser ist
das folgenlos — ein `Origin`-Kopf trägt nie einen Pfad —, aber ein Ausdruck,
der mehr erlaubt als er soll, ist eine Zusage, die niemand geprüft hat. Jetzt
ein Hostname-Muster; nachgemessen an Deploy-Vorschauen und zwei Fälschungen.

Dass **jede** Netlify-Adresse erlaubt ist, bleibt Absicht: Dort liegen die
erzeugten Kundenseiten, und sie einzeln zu pflegen hieße, jede neue Kundenseite
mit einem Deploy des Backends zu bezahlen. Gefährlich wäre es mit Cookies — die
Anmeldung läuft über ein `Bearer`-Token, das eine fremde Seite nicht lesen
kann. Der Vorbehalt steht im Code.

**Die Standardadresse im Skript ist Frankfurt, nicht die aus dem Auftrag.**
BUCH-09 nennt `claude-code-znq2.onrender.com`; dieser Dienst liegt in Oregon,
ist seit dem Umzug (L-34) suspendiert und antwortet mit 503.

---

## 4 · L-105 — vier Blöcke, von 96 auf 72

Zu jeder Route ohne Aufrufer stellt L-105 **eine** Frage: fehlt der Knopf,
oder ist sie überflüssig?

| Stand | ungerufen | erklärt | beurteilt |
|---|---:|---:|---:|
| Ausgangspunkt | 96 | 14 | 0 |
| nach dem Aufräumen der Messung | 84 | 28 | 31 |
| nach dem `projects`-Block | 84 | 28 | 50 |
| nach den Entscheidungen | **72** | 28 | 50 |

**Die Zahl fiel zuerst, ohne dass etwas verschwand.** Die Messung führte
Webhooks, Mail-Links und Betriebsanzeigen als offene Fragen; jede ist jetzt
mit Beleg erklärt. Zwischendurch **stieg** sie von 86 auf 88 — der entfernte
Rückfall war der einzige „Aufrufer" von `/api/usercards/`, und die höhere Zahl
war die ehrliche.

### Eine Ausnahme, die ihren Grund überlebt hatte

Das Werkzeug nahm zwei Audit-Routen dauerhaft aus der Prüfung: Die
WebSprint-Landingpage liege außerhalb des Repos und rufe sie. **An der
Live-Seite nachgemessen: kein einziger `/api/`-Aufruf** — sie bettet das
Widget als iframe ein, und das ruft nur `/api/widget/*`.

> Eine Ausnahme, die ihren Grund verloren hat, ist schlimmer als keine: Sie
> nimmt etwas dauerhaft aus der Prüfung, und sie gibt keinen Alarm.

### Der fünfte Ort von L-26

`GET /api/leads/customers` wählte über `status == "won"` und übersah jeden
Betrieb, den jemand auf „Kunde" gesetzt hat. Dieselbe Reparatur wurde am
19.08. zweimal und am 31.08. einmal gemacht — **hierher kam sie nie, weil
niemand die Route ruft.** Das ist L-105 in einem Satz.

### Ein ganzes Merkmal ohne Auslöser

Vier Routen erzeugen, zeigen und wählen drei Website-Entwürfe. Die
**Kundenseite war fertig gebaut und angeschlossen** — das Portal zeigt „🎨 Ihre
3 Website-Entwürfe sind bereit!" samt Vorschau. Nur konnte kein einziger
entstehen: `POST /generate-versions` ist der einzige Schreiber von
`website_versions` und kam im ganzen Repo nur in seiner eigenen Definition vor.

**Warum es niemandem auffiel:** Das Portal hat den ehrlichen Riegel
`if (versions.length === 0) return null`. Der Abschnitt versteckt sich. Der
Kunde sah nie ein leeres Versprechen — und genau deshalb merkte niemand, dass
er es nie sehen würde.

> **Ein gut versteckter Mangel ist schwerer zu finden als ein sichtbarer.**

Der Schritt „Drei Entwürfe" hängt jetzt in Phase 3. Ebenso angeschlossen: die
**Reststunden der Marge** — elf Kennzahlen wurden berechnet, eine angezeigt.

### Sieben Routen gelöscht, rund 280 Zeilen

Zwei Doppelwege, der letzte Schreiber einer eingefrorenen Spalte, das
abgelöste Sitemap-Parallelsystem. **Die Spalten bleiben stehen** — leeren wäre
Datenverlust ohne Not.

**Beinahe hätte ich die falsche gelöscht.** Der naheliegende Vergleich für
`/projects/{id}/scrape` war der Crawler — und der holt **Inhalte**, keine
Markenfarben. Erst der Blick darauf, was beide *schreiben*, brachte die
richtige Gegenstelle.

> **Zwei Routen mit demselben Wort im Namen sind noch lange nicht dieselbe
> Sache.**

---

## 5 · L-101 — zu Ende gebaut

Der Eintrag hing seit dem 23.08. und nannte drei fehlende Dinge. Alle drei
stehen.

| | |
|---|---|
| **Vertrag** | welches Abo ab wann gilt, mit Historie |
| **Restzahl** | Kontingent minus Verbrauch, aber nur mit Vertrag |
| **Termingeber** | Re-Audit vierteljährlich (Pro) bzw. jährlich (Basic) |
| **Abrechnung** | monatliche Aufstellung — **per Rechnung** (Entscheidung David) |

**Vier Entscheidungen stecken im Detail:** Monate statt Tage · `end_monat` ist
der letzte **gültige** Monat · kündigen heißt beenden, nicht löschen · eine
Überschreitung wird **gezeigt statt auf null gekappt**.

**Die teuerste Regel ist der Wechsel:** Er schreibt eine neue Zeile, damit ein
später aufgerufener Juli mit dem Kontingent rechnet, das *im Juli* galt.

**Zwei Läufe geben kein Geld aus.** Weder der Termingeber noch die Abrechnung
lösen etwas aus, das kostet: Der Wert des Re-Audits ist der Vergleich, das
Guthaben ist knapp, und eine Rechnungsnummer ist fortlaufend und lässt sich
nicht zurücknehmen.

### Drei Zahlen, die ich falsch gebaut hatte

| | Datenblatt | was ich gebaut hatte |
|---|---|---|
| ABO-BAS Änderungen | 30 min = **0,5 h** | 0,0 h |
| ABO-PRO Änderungen | 90 min **„statt 30"** = 1,5 h | 2,0 h |
| ABO-BAS Re-Audit | **jährlich** | vierteljährlich |

Alle drei kamen aus dem **Fließtext des Lagebild-Eintrags** statt aus
`docs/produkte/abo-und-geo.md`. Jeder kostet Geld, in verschiedene Richtungen:
Mit 0,0 für Basic wäre jede Minute eine Überschreitung gewesen; mit 2,0 für
Pro hätten wir monatlich eine halbe Stunde verschenkt.

Der teuerste Halbsatz war **„(statt 30)"** — daraus wurden bei mir 30 + 90.

> **Ein Summentext ist keine Produktdefinition.**

`tests/test_abo_datenblatt.py` liest jetzt das Datenblatt und rechnet die
Minuten selbst um. Ein Test, der `1.5 == 1.5` prüft, hält nur fest, was jemand
zweimal geschrieben hat.

---

## 6 · L-17 — die 20 % waren etwas ganz anderes

Der Messlauf am Abend hat zwei meiner eigenen Aussagen umgeworfen.

**„Text auf Bild oder Verlauf"** stand seit dem 23.08. im Eintrag. Gemessen:
**kein einziges Zeichen** steht auf einem Bild. Die Pixelmessung, die ich
dafür baute, ist richtig — sie löst einen Fall, den es hier nicht gibt.

Die echte Ursache war **halbdurchsichtige Schriftfarbe**: `alsRgb` gab bei
Alpha < 1 auf und warf sie in denselben Zähler wie Bilder. Derselbe
Denkfehler — die Messung gab auf, wo sie rechnen konnte.

**Und dahinter lag ein echter Mangel.** Aus 0 % gefallen wurden **21,3 %**:

| Stelle | gemessen | nötig |
|---|---:|---:|
| Gruppenbeschriftung der Seitenleiste (561 Zeichen) | 3,97 | 4,5 |
| Ausklapppfeile | 2,20 | 4,5 |
| Abschnittsbeschriftung | 2,08 | 4,5 |
| Design-Umschalter | 3,16 | 4,5 |

Auf **jeder** Seite, seit es die Seitenleiste gibt. Nachgemessen nach der
Korrektur: **0 von 3.203 Zeichen** unter der Schwelle, unentscheidbar 9 statt
690.

> **Ein Zähler, der ausweicht, ist schlimmer als einer, der schweigt — er
> sieht aus wie eine Antwort.**

---

## 7 · Der rote Lauf

Der Push um 19:28 war rot. Der Browsertest „Entwurf auf die Seite übernehmen"
fand die Design-Ansicht **gesperrt** vor; der Deploy wurde übersprungen.

**Die Ursache stand als Warnung drei Zeilen über der Stelle, an der ich sie
gebaut habe.** Die Schrittkette sperrt alles hinter dem ersten Schritt, der
weder erledigt noch als optional gekennzeichnet ist. Mein neuer Schritt hatte
weder das eine noch das andere. Genau das ist am 21.08. schon passiert, und
der Kommentar daneben beschreibt es ausführlich.

**Zwei weitere eigene Fehler auf dem Weg zur Reparatur:**

* **Eine bestehende Testdatei überschrieben.** `schrittkette.test.js` gab es
  mit acht Tests; das `ls` hatte sie mir angezeigt. Aufgefallen **nur an der
  Zahl** — 572 statt 577, obwohl ich drei hinzugefügt hatte. Eine gestiegene
  Summe hätte es verdeckt.
* **Der erste Wächter maß seine eigene Schleife.** Er las die Schlüssel des
  Rückgabewerts, um zu bestimmen, welche Schritte eine Heuristik haben — aber
  die Sperrschleife am Ende füllt jeden Schlüssel auf. Grün, egal was man
  einfügte. Gefunden nur durch die Gegenprobe.

Alle drei waren **grün**. Gefunden hat sie die Gegenprobe und der Browserlauf
— die späteste und teuerste Stelle.

---

## Eigene Messfehler

* **Ein „Build grün", das ein altes Verzeichnis las.** Der erste Frontend-Bau
  lief mit `CI=true`, scheiterte an 57 vorbestehenden Lint-Warnungen — und
  `build/index.html` gab es trotzdem, aus einem früheren Lauf. Die CI baut
  bewusst mit `CI: false` über `scripts/frontend-bauen.sh`; so gebaut ist er
  wirklich grün. **Die Anwesenheit einer Datei ist kein Beleg für den Lauf, der
  sie erzeugt haben soll.**
* **`app.routes` zählt falsch.** Die Prüfung der neuen Routen meldete null
  Treffer — auch für die längst vorhandenen. Unter Starlette 1.4 liefert
  `app.routes` nur ~83 Einträge; gezählt wird über `app.openapi()['paths']`
  (420). Stand in [[lokale_entwicklungsumgebung]], und ich bin trotzdem
  hineingelaufen.

---

## Was heute die Arbeit geprüft hat

| Wächter | Was er meldete |
|---|---|
| `test_frontend_adressen` | mein verschachtelter fetch-Ausdruck war nicht lesbar |
| `test_zugriffsschutz_bestand` | der neue offene Endpunkt (56 → 57, jetzt mit Grund) |
| `menueZiele.test.js` | grün — Menüpunkt und Route passen zueinander |
| `test_schriftgroessen` (neu) | rot bei Rückdrehung, mit Datei, Zeile und Wert |

Drei von vier haben angeschlagen. Das ist der Zweck.

---

## Zahlen

| | |
|---|---|
| Commits | **16**, alle gepusht |
| CI-Läufe | 9, davon **einer rot** (E2E, behoben) |
| Backend-Tests | 3.183 grün, 2 übersprungen (beide mit Grund) |
| Frontend-Tests | 579 grün |
| ruff | sauber |
| Frontend-Bau | sauber, kein gebrochener Import |
| Lagebild | 147 Lücken · 20 offen · 4 teilweise · **P0 offen: 0** |

---

## Wo P2 jetzt steht

**Heute geschlossen bzw. zu Ende gebaut:** L-17 (Schriftgrößen **und**
Kontrast auf 0 %), L-101 (Vertrag, Restzahl, Termingeber, Abrechnung),
L-115 (BUCH-07 und BUCH-09), L-114 und L-81 als *terminiert* eingeordnet.

**Offen: 18** statt 20 heute früh. P0 und P1 sind weiter leer.

### Zwei Knöpfe warten nicht mehr

Die drei Website-Entwürfe und die Reststunden der Marge sind angeschlossen —
zwei fertige Merkmale, die nur einen Auslöser brauchten.

### Was jetzt bei David liegt

| | |
|---|---|
| **Abo-Preise bestätigen** | 79 / 149 € stehen im Datenblatt als „⚠️ Annahme"; jede Abrechnung sagt das dazu |
| **Adresse für BUCH-08** | danach ist BUCH-10 eine halbe Stunde, und `CORS_ALLOWED_ORIGINS` braucht einen Handgriff |
| **`SELECT count(*) FROM usercards`** | davon hängen 18 Routen ab (L-106) |
| **KI-Guthaben** | L-58 und L-85 hängen daran |
| **Anrede und Stückzahl beim Buchkauf** | die CSV-Spalte bleibt sonst leer, die Menge fest bei 1 |
| **Löschen freigeben** | 3 Akademie-Doppelwege, `geo/ki-anbieter` **oder** `diagnostics/config` |

### Was ohne David baubar ist

L-105 weiter: **34 von 84 Routen** noch unbeurteilt, davon `leads` (10) der
nächste Block. `usercards`/`customers` (18) hängen an der Produktivmessung.


## Offener Handgriff

`CORS_ALLOWED_ORIGINS` an den Render-Diensten um die Buch-Landingpage
ergänzen — **sobald BUCH-08 eine Adresse hat**, nicht vorher. Ob sie angekommen
ist, sagt danach die erste Zeile im Render-Log nach dem Neustart:

    CORS erlaubte Origins: [... 'https://…']

oder ein Aufruf:

    bash scripts/check-cors.sh https://die-neue-adresse
