# Test- und Entwicklungsabhaengigkeiten.
# Bewusst getrennt von requirements.txt, damit der Produktiv-Build auf Render
# schlank bleibt.
#
#   pip install -r requirements.txt -r requirements-dev.txt

-r requirements.txt

pytest==8.3.3
pytest-cov==5.0.0
