{
  "name": "kompagnon-e2e",
  "private": true,
  "description": "Browser-Tests. Bewusst ein eigenes Paket, damit der Frontend-Build nicht mit Playwright belastet wird.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "1.49.1"
  }
}
