import React, { useState, useRef, useEffect, useCallback } from 'react';
import EmailEditor, { EditorRef, EmailEditorProps } from 'react-email-editor';
import { ArrowLeft, Save, Send, X, Clock, LayoutTemplate } from 'lucide-react';
import { User } from '../types';

// TODO: Vollständigen Inhalt aus Silva-Viridis-Portal übernehmen (582 Zeilen)
// Enthält 3 Unlayer Template-Designs + Editor-Komponente

const NewsletterEditor: React.FC = () => {
  return <div>NewsletterEditor — Platzhalter, vollständiger Code muss noch übernommen werden.</div>;
};

export default NewsletterEditor;
